# Wellbeing Spaces

## Tech Stack
Next.js 14 (App Router) · TypeScript · Tailwind · Supabase (Frankfurt) · Vercel (fra1, auto-deploy main)

## Kern
Kunden → Projekte → Räume → Produkte. `raum_id = NULL` = Produktbibliothek.
Admin sieht alles (EP/Marge/Provision). Kunde nur Freigabelink `/freigabe/[token]`.

## DB-Tabellen
`kunden`, `projekte`, `raeume`, `partner`, `partner_konditionen`, `produkte`, `raum_produkte`, `produktstatus`, `freigabe_tokens`, `einstellungen`, `zeiterfassung`
Portaltabellen: `client_users`, `client_nachrichten`, `client_dokumente`, `client_aktivitaeten`, `client_benachrichtigungen`
Letzte Migration: 045 (raumplaner) — noch nicht in Supabase ausgeführt! Migrations in `/supabase/migrations/`.
**Portal-Auth:** Eigenes System (bcrypt + session_token Cookie), unabhängig von Supabase Auth. `createAdminClient()` für alle Portal-Operationen (bypasses RLS).
**einstellungen-Schema (echte DB):** `id` (uuid), `key` (text, unique), `value` (text), `created_at`, `updated_at` — NICHT `schluessel`/`wert`!

## Regeln
- Server Actions in `src/app/actions/`. Supabase admin nur serverseitig (`admin.ts`).
- Interne Felder (`einkaufspreis`, `marge_prozent`, `provision_prozent`, `notizen_intern`) NIE an Kunde.
- Soft Delete (`deleted_at`). UI-Texte Deutsch. Tailwind only, kein inline CSS.
- Kategorien: `einstellungen`-Tabelle, Format `Name|IconName` (z.B. `Möbel|Sofa`).
- Preislogik: EP netto + Marge% → VP netto; VP brutto = VP × 1,19; Provision = VP × Provision%.

## Design
Wellbeing Green (#445c49) aktiv. Sidebar: bg-[#445c49] (forest green), Syne-Font. Desktop-first, ruhig/reduziert.
Farbpalette: wellbeing-green (#445c49), wellbeing-green-light (#94c1a4), wellbeing-green-dark (#2d3e31), wellbeing-cream (#f6ede2), wellbeing-terracotta (#823509), wellbeing-sand (#cba178).

### Design-Konsistenz (DAUERHAFTE REGEL — wichtig!)
**Alle Dashboard-Listenseiten MÜSSEN exakt das gleiche Layout verwenden** — kein eigenes Hero-Band, kein Sonderdesign. Referenz: `/dashboard/projekte/page.tsx`. Pattern:
1. **`<StickyPageHeader>`** oben (weißer BG, Titel + Count + optional Subtitle + Action-Button rechts) — NIE durch Gradient-Hero ersetzen.
2. **Filter-Pills-Reihe** im Projekte-Stil: `bg-wellbeing-green text-white` für aktiv, `bg-white text-gray-500 border border-gray-200` für inaktiv, mit Count-Badge `bg-white/20` aktiv / `bg-gray-100` inaktiv.
3. **Toolbar**: 340px Suchfeld (`focus:ring-wellbeing-green/20 focus:border-wellbeing-green-light`), Counter `text-gray-400`, rechts Grid/List-Toggle (`border border-gray-200 rounded-lg overflow-hidden`).
4. **Cards** auf weißem BG (`bg-white border border-gray-200 rounded-xl shadow-sm`), Hover `hover:shadow-md hover:border-gray-300`. Avatar-Tile links (10×10 rounded-lg, farbig nach `avatarFarbe(seed)` aus pastelligen Tones), Titel + Subtitel mittig, Datum/Status oben rechts. Footer mit Border-Top + Meta-Infos.
5. **Hintergrund** der Page: hell/neutral (kein dunkles Gradient-Band). Empty-States als weiße Cards mit Border, gleicher Stil.
6. **Modals/Editor-spezifische Tools** (Raumplaner/Moodboard-Editor) DÜRFEN dunkles UI haben — Listen/Übersichten NICHT.

→ Wenn eine neue Übersichtsseite gebaut wird: erst Projekte-Page anschauen, dann 1:1 dieses Pattern verwenden. Nicht "kreativ werden". User hat ausdrücklich gefordert, dass alle Dashboards einheitlich aussehen müssen.

## Offen
- **Migrations-Status:** alle bis 108 in Production eingespielt. Workflow ab jetzt: neue Migrationen liegen direkt in `supabase/migrations/`, nach erfolgreichem Einspielen in Supabase per `git mv` in `supabase/migrations/applied/`. Siehe `supabase/migrations/README.md`.
- **Auto-Deletion archivierter Kunden nach 30 Tagen** (Cron-Job via Supabase Edge Function oder Vercel Cron): `deleted_at < now() - interval '30 days'` → Hard-Delete. Aktuell nur manuell möglich.
- **Raumplaner-Reduktion Phase 2+** (siehe Plan-Datei): Datei-Split (5535 → ~1500 Core + Module), Touch-Gesten (Pinch-Zoom, 1-Finger-Pan, Long-Press), Responsive-Layout (Bottom-Sheet Möbel, Drawer Eigenschaften, Phone-Blocker), Render-Batching (scheduleRender-Helper), Grid-Pattern auf Canvas-Background, Möbel-Suche mit fuse.js, Elektro-Schnellleiste.

## Session-Log
- S12: Notizen (Migration 015), Logo-Upload (Migration 016), Projektdetail-Stats, FreigabeLinkKarte.
- S13: Login-Seite Redesign (DepthStack-Icon, Syne, gepunkteter Hintergrund, Icon-Inputs, Loader-Animation).
- S14: Kundenfreigabe mobil – großes Produktbild, Touch-Buttons (py-3.5, flex-col→row), Mini-Donut im Header, einklappbare Beschreibung, Preis-Grid.
- S15: Bibliotheksprodukt zuweisen – ProduktZuweisenModal (Projekt+Raum-Dropdown), Button in Grid+Tabelle, Action produktZuRaumZuweisen.
- S16: MwSt. dynamisch – getMwstSatz() aus einstellungen-Tabelle, alle Produkt-Berechnungen (Formular, Tabelle, Freigabe, CSV-Export) lesen Wert aus DB statt hardcoded 19%.
- S17: PDF-Export – PdfExportButton (jspdf + jspdf-autotable, client-seitig), Button auf Projektdetailseite neben CSV, A4-Portrait mit Indigo-Header, Produkttabelle, Status-Farben, Gesamtsumme, Seitenzahl.
- S18: Drag & Drop Räume – SortableRaumListe (@dnd-kit), GripVertical Handle, optimistisches UI, updateRaumPositionen Action, Migration 017.
- S19: Drag & Drop Produkte – SortableProduktTabelle (@dnd-kit), GripVertical Handle links pro Zeile, updateProduktPositionen Action, Migration 018. Fix 2 (ProduktZuweisenModal in Bibliothek) war bereits fertig (S15).
- S20: Kategorien-Bugs gefixt (Admin-Client + maybeSingle(), Migration 019 Seed). RaumHinzufügen mit Raumtypen-Kacheln (Icons, aufklappbares Panel). ChatGPT-Kontext-Paket in chatgpt_project_context/ erstellt (17 Doku-Dateien, keine bestehenden Dateien verändert).
- S21: Bestellstatus-Flow (Migration 021, bestellstatus_enum, SortableProduktTabelle mit Select-Dropdown + optimistischem UI). Vollständiges Rebranding WBC Studio → Wellbeing Spaces: tailwind.config.ts wellbeing-Farbpalette, alle indigo-* Klassen → wellbeing-green/light/dark/cream, Hex-Farben ersetzt, Sidebar #0F1117 → #445c49, DepthStackIcon weiße Fills, package.json, CLAUDE.md.
- S22: Fix einstellungen-Spaltennamen: echte DB hat `key`/`value` statt `schluessel`/`wert` (Migrations-Drift). Alle Supabase-Queries in einstellungen.ts korrigiert.
- S23: Onboarding-System (TEIL 1-3): Umbenennung Anfragen→Onboarding, Status-Badges, Link-Löschen, Vorlagen-System (Migration 025, onboarding_vorlagen JSONB, CRUD-UI VorlagenVerwaltung, dynamisches Formular). Projekt-Archivierung + Duplikation (Migration 026, ProjektAktionenButtons, DuplizierenModal). Whitelabel/Branding (Migration 027, branding-Tabelle, BrandingEditor mit Farbpickern/Logo-Upload/Font-Selector/Live-Vorschau, Branding-Tab in Einstellungen, Branding auf Freigabe- und Onboarding-Seiten).
- S24: Live Kunden-Konfigurator (Migration 028, konfigurator_sessions + auswahl, KonfiguratorLinkKarte, /konfigurator/[token] mobiloptimiert, 4 Aktionen, Budget-Tracking, Abschluss-Modal) + Projekt-Timeline / Gantt (Migration 029, timeline_events + liefertermin auf produkte, TimelineView mit Gantt + Liste, EventModal, Meilenstein-Rauten, NaechsteEvents Widget).
- S25: Interaktives Handbuch – /dashboard/einstellungen/handbuch (HandbuchClient mit 3-Spalten-Layout, linke Kapitel-Sidebar, rechte "Auf dieser Seite"-Sidebar, Live-Suche + CMD+K Shortcut, 16 Kapitel mit vollständigen Inhalten, InfoBox/Badge/Kb Komponenten, Paging-Navigation).
- S26: Projekt-Status Buttons (ProjektStatusButtons Client-Komponente, optimistisches UI, Bestätigung für Abgeschlossen, Farben offen=grau/in_bearbeitung=amber/freigegeben=blau/abgeschlossen=grün). Client Dashboard / Kunden-Portal: Migration 030 (client_users + 4 Portal-Tabellen), eigene bcrypt-Auth mit session_token Cookie, /portal/* Routen (landing/login/einladung/[token]/dashboard/projekte/[id]/profil), KundenPortalSection Admin-UI auf Kundenseite, portalBenutzerAbrufen auf Projektseite, Middleware-Schutz für Portal-Routen.
- S27: Logo/Favicon-Austausch (logo-klein/mittel/gross.png + logo.svg in /public, Nav/Footer/Login/Freigabe aktualisiert, NavSidebar-Icon bleibt weiß). Fix: Offene Freigaben Link → /dashboard/freigaben. Produkte-Bibliothek überarbeitet: Freigabe-Status entfernt, Partner-Filter hinzugefügt, Kompakt-Ansicht (3. View, 4-8-Spalten-Grid). ProduktFormular komplett neu: 2-Spalten-Layout, AutoFill-Modal (URL-Scraper mit Cheerio + JSON-LD), TagsInput, BilderGrid (Drag&Drop, bis 5 Bilder), 10 neue Felder (lieferzeit/maße/material/farbe/artikelnummer/verfuegbarkeit/tags/bilder_urls), Notizen in rechter Spalte. Migration 031 (10 neue Spalten auf produkte-Tabelle). Neue Seite /dashboard/produkte/[id]/bearbeiten. dangerouslySkipPermissions konfiguriert.
- S28: Build-Fixes (ESLint unused import + ternary-als-statement in ProduktFormular). Landing Page: Logos größer (logo-gross.png, 36/40px), Header-Farbstreifen entfernt (bg-white/border statt translucent), Badge-Bug Pricing gefixt, max-width 1300px für alle Sections, WhyWBC-Texte verbessert, ProblemSolution neutral/brand (kein amber/rot), -8-Std. Minus-Zeichen größer, Testimonials entfernt. Projekt-Detailseite: Toolbar mit ⋮-Dropdown (Duplizieren/Archivieren/Löschen), 100vh 2-Spalten-Layout (60%/40% je unabhängig scrollend), Raumtypen als Dropdown aus Kategorien (Migration 032, icon-Spalte auf raeume), Räume als Cards mit Stats (SortableRaumListe), PIN-Schutz nur wenn Token vorhanden + Toast-Feedback. Einstellungen komplett überarbeitet: neue Tabs Profil/Workspace/Branding/Team/Freigaben/Benachrichtigungen/Abrechnung/Handbuch, Team-Bug (aktueller User mit "Du"-Badge immer sichtbar), Handbuch-Bug gefixt (inline im Tab, kein Seiten-Reload), Branding inline (kein separater Route-Sprung mehr).
- S29: Projekt-Detailseite überarbeitet: Deadline-Badge mit Countdown im Header (Migration 033), Kunde-Karte prominent (Name/Email/Telefon), Kunden-Portal-Section entfernt, Status vereinfacht auf 3 Zustände (Aktiv/Warten auf Kunde/Abgeschlossen, in_bearbeitung → offen gemappt). ProduktFormular: VK Brutto-Eingabe oben (Interne Kalkulation einklappbar), Menge-Feld entfernt. RaumHinzufügen: nur Dropdown (kein Freitext), Fehler besser geloggt. SortableRaumListe: Budget-Fortschrittsbalken pro Raum. Migration 033 (deadline + verantwortlicher_id auf projekte, budget auf raeume, projekt_aktivitaeten-Tabelle) — muss manuell in Supabase ausgeführt werden.
- S30: Team-Tab in Einstellungen: Scrollbar entfernt (flex-wrap), Tab immer sichtbar (nicht mehr adminOnly), vollständige Mitgliederliste mit Avataren/Rollen/⋮-Dropdown/Einladungsmodal. Onboarding komplett überarbeitet: 12 Feldtypen (email, telefon, url, ja_nein, bewertung, skala neu) in gruppiertem Custom-Dropdown, Tag-Input für Optionen (Enter/Komma hinzufügen), Sektionen/Gruppen mit klappbaren SektionsBlöcken + Per-Sektion "Frage hinzufügen", VorschauModal (Kundenansicht), Frage-Duplizieren (⋮-Menü), 3 Beispiel-Vorlagen als Picker beim Erstellen (🏢 Gewerbe/🏠 Privat/🏨 Hotel), Modal mit max-h-[88vh] + fixem Header/Footer + scrollbarem Body, besseres Empty-State in OnboardingTabelle. Migration 034 (sektionen JSONB auf onboarding_vorlagen) — muss manuell in Supabase ausgeführt werden.
- S31: Multi-Tenancy-Grundlage (Migration 036): Tabelle `organisationen` (Abo-Pläne trial/starter/professional/enterprise, max_projekte/max_mitglieder), `organisation_id` (nullable) auf 24 Tabellen, Indizes auf 5 Kerntabellen, Helper-Funktion `get_user_org_id()` (SECURITY DEFINER), alle bestehenden RLS-Policies gedroppt und durch org-scoped Policies ersetzt, Anon-Policy für freigabe_tokens erhalten, Admin-only Update-Policy für organisationen, Tabelle `audit_log` mit RLS. Code-Integration: `getOrganisationId()` in server.ts, alle 14 Insert-Actions mit `organisation_id` versehen, `organisation_id?: string | null` in alle relevanten Types-Interfaces eingetragen. Registrierungs-Flow: `organisation.ts` (organisationErstellen/userMitOrgVerknuepfen/getAktuelleOrganisation/migriereBestehendeDaten), Auth-Callback erweitert (auto-Org-Erstellung bei erstem Login), `Organisation`-Interface + `AboPlan`-Type in types.ts.
- S32: Kategorien-Tabelle (Migration 037): neue `kategorien`-Tabelle (id/org/typ/name/icon/reihenfolge, RLS org-scoped), FK-Spalten kategorie_id/raumtyp_id/projektart_id auf produkte/raeume/projekte. Code: `getKategorien`/`addKategorie`/`updateKategorie`/`deleteKategorie`/`checkKategorieUsageById` in einstellungen.ts. KategorienVerwaltung.tsx auf `Kategorie[]`-Props umgestellt (useTransition statt useFormState). Kategorien-Page lädt via getKategorien(). Alle 4 ProduktFormular-Pages + 2 ProjektFormular-Pages + projekte/[id]/page + partner/[id]/page + produkte/page auf neue Actions umgestellt — keine CSV-Strings mehr.
- S35: Service-Abrechnung + Zeiterfassung (Migration 040): 4 neue Spalten auf projekte (service_modell/service_pauschale/service_stundensatz/produkt_budget). Neue Tabelle `zeiterfassung` (org/projekt/user/datum/stunden/beschreibung/abrechenbar, RLS org-scoped). Neues `zeiterfassung.ts` mit 5 Actions (zeitEintragen/zeitAktualisieren/zeitLoeschen/getZeiterfassung/getZeitSumme). ProjektFormular um Abschnitt "Service & Abrechnung" erweitert (Modell-Dropdown, Pauschale/Stundensatz, Produkt-Budget mit Hinweistext). projektAnlegen/projektAktualisieren speichern neue Felder. ProjektDetailPage: Budget-Ring zeigt produkt_budget (mit Fallback auf gesamtbudget) + Service-Kosten-Zeile + Gesamtsumme. Neuer ZeiterfassungBlock (Client-Komponente, Inline-Formular, Eintrags-Liste mit Lösch-Buttons, Summenzeile mit €-Betrag, nur sichtbar bei stundensatz-Projekten).
- S34: Partner-Konditionen (Migration 039): neue `partner_konditionen`-Tabelle (org/partner/name/typ/wert/staffelung JSONB/kategorie_werte JSONB/gueltig_von-bis/zahlungsziel/skonto/aktiv, 7 Typen, RLS org-scoped, updated_at-Trigger). Partner-Tabelle um 5 Felder erweitert (partner_typ/zahlungsziel_tage/iban/ust_id/bewertung). Neue Client-Komponente `PartnerKonditionenBlock` (Modal zum Anlegen/Bearbeiten/Löschen, JSON-Felder für Staffelung/Kategorie-Werte). `PartnerFormular` um neue Felder ergänzt (Typ-Dropdown, Stern-Bewertung, IBAN, USt-Id, Zahlungsziel). Partner-Detailseite zeigt Konditionen-Block und neue Felder in Kontakt-Karte.
- S33: Produkt-Referenz-System (Migration 038): neue `raum_produkte` Junction-Tabelle (org/raum/produkt/menge/verkaufspreis_override/reihenfolge, RLS org-scoped). Datenmigration aus produkte.raum_id. Neues `raum-produkte.ts` mit 5 Actions (produktZuRaumHinzufuegen/produktAusRaumEntfernen/raumProdukteAktualisieren/updateRaumProduktPositionen/getRaumProdukte). Types: RaumProdukt + RaumProduktMitDetails. SortableProduktTabelle komplett auf RaumProduktMitDetails umgestellt (Preis-Override-Badge, Entfernen statt Löschen). Raum-Detailseite neu: lädt via getRaumProdukte(), filterUndSortieren auf RaumProduktMitDetails. Bibliothek zeigt "In X Räumen"-Badge statt Raum/Projekt. getProjektStats/getRaumStats/getProdukteForPdf auf raum_produkte JOIN umgestellt. ProduktZuweisenModal auf produktZuRaumHinzufuegen umgestellt.
- S39: Angebotsmodul (Migration 044): Tabelle `angebote` (org/projekt/kunde/nummer/titel/einleitung/positionen JSONB/summen/mwst/rabatt/status 5 Zustände/gueltig_bis/anmerkungen/agb_text, RLS). SQL-Funktion `naechste_angebotsnummer(org_id)` für AG-YYYY-NNN pro Org. Types: Angebot, AngebotStatus, AngebotPosition. Neues `angebote.ts` mit 8 Actions: angebotErstellen (mit auto-Nummer via RPC), angebotAusProduktliste (lädt raum_produkte und generiert Positionen), CRUD, statusAendern, berechneAngebotSummen. Seite /projekte/[id]/angebote mit AngeboteClient: Liste, Formular (dynamische Positions-Tabelle, Live-Summen, Rabatt, MwSt, AGB), Detail-Ansicht, Status-Dropdown, Löschen. ProjektDetailPage: Angebote-Link in Toolbar.
- S38: Vertragssystem (Migration 043): Tabellen `vertrags_vorlagen` (org/name/beschreibung/inhalt_html/platzhalter JSONB/kategorie/ist_standard/version, RLS) + `vertraege` (org/vorlage/projekt/kunde/titel/inhalt_html/status 6 Zustände/signatur_felder/gesamtwert/gueltig_bis, RLS). Types: VertragsVorlage, Vertrag, VertragStatus, VertragsVorlageKategorie. Neues `vertraege.ts` mit 9 Actions (CRUD Vorlagen, vertragErstellen mit Platzhalter-Ersetzung, Status ändern etc.). Platzhalter: 12 Schlüssel (firmenname, kunde_*, projekt_*, budget_felder, datum_heute, deadline). Seite `/projekte/[id]/vertraege` mit VertraegeClient (Status-Dropdown, Vorschau-Modal, Löschen). Einstellungen: neuer Tab "Vorlagen" mit VertragsVorlagenVerwaltung (CRUD, Platzhalter-Einfüge-Hilfe, HTML-Editor). ProjektDetailPage: "Verträge"-Link in Toolbar.
- S37: Kunden-Kommunikationslog (Migration 042): neue `kommunikation`-Tabelle (org/kunde/projekt/typ/richtung/betreff/inhalt/kontaktperson/datum/dauer_minuten/follow_up_datum/erledigt, 6 Typen, RLS org-scoped). Types: KommunikationTyp, KommunikationRichtung, Kommunikation-Interface. Neues `kommunikation.ts` mit 6 Actions (kommunikationAnlegen/Aktualisieren/Loeschen/getKommunikation/getOffeneFollowUps/followUpErledigen). Neue Client-Komponente `KommunikationBlock` (Timeline-Ansicht, Typ-Icons, Inline-Formular, Filter-Tabs, Follow-up-Badge mit Fälligkeits-Farbe, Erledigen-Button). Kunden-Detailseite: KommunikationBlock unterhalb Projekte in rechter Spalte.
- S36: Produkt-Varianten (Migration 041): 3 neue Spalten auf produkte (ist_variante/eltern_produkt_id/varianten_attribute JSONB), neue `varianten_definitionen`-Tabelle (org/produkt/attribut_name/optionen TEXT[]/reihenfolge, RLS org-scoped). Types: Produkt erweitert + VariantenDefinition-Interface. 6 neue Server Actions in produkte.ts: varianteAnlegen (kopiert Elternprodukt, setzt ist_variante=true, generiert Name aus Attributen), variantenDefinitionSpeichern (upsert), getVariantenDefinitionen, getVarianten, variantenDefinitionLoeschen.

- S41: Professioneller PDF-Export (TEIL 1-4): Neue API-Routen GET /api/angebote/[id]/pdf + /api/vertraege/[id]/pdf (server-seitig, jsPDF + autoTable, Auth via createClient(), arraybuffer Response). Shared src/lib/pdf-helpers.ts (pdfEur/pdfDatum/pdfHeute, WB_GREEN-Konstanten, logoAlsBase64 via fetch, htmlZuBloecke HTML→Strukturblöcke). Angebot-PDF: Firmen-Header (Logo+Kontakt rechts, grüne Trennlinie), ANGEBOT-Titel mit Nummer, Kunde+Datum 2-Spalten, Einleitung, Positions-Tabelle WB-Green-Header, Summen-Block, Anmerkungen, AGB klein, Footer Seitenzahl. Vertrag-PDF: gleicher Header, Vertragsinhalt als Blöcke (h1/h2/h3/p/li/hr), Unterschriftsbereich 2-Spalten (Auftraggeber/Auftragnehmer, je Linie Datum+Unterschrift). Download-Buttons: AngeboteClient + VertraegeClient jeweils Download-Icon-Button + Detail-View-Button. PdfExportButton: WB-Green statt Indigo, Logo-Fetch aus Branding-Props, Firmenkontakt im Header.
- S40: Dashboard komplett überarbeitet (Migration 045 nicht nötig, reine Code-Änderung): Neue `DashboardWidgets.tsx` mit 6 Props-Komponenten (KpiKarte, NaechsteDeadlines, OffeneFollowUps, BudgetUebersicht, LetzteAktivitaeten, LetzteProjekte). Altes Dashboard (DonutChart/BalkenChart/LinienChart/OffeneFreigaben) ersetzt durch 4-Zeilen-Layout: ROW 1 KPI (Aktive Kunden/Laufende Projekte/Offene Angebote/Monatsumsatz), ROW 2 Deadlines+FollowUps (60-Tage-Fenster + 7-Tage-Fenster, graceful fallback), ROW 3 Budget+Aktivitäten (Top-5-Projekte + audit_log, graceful fallback), ROW 4 Letzte-Projekte-Tabelle mit Deadline-Countdown-Spalte.

- S42: Kritische Server-Fehler behoben (app-weiter Vercel-Crash): meineRolleAbrufen() in try/catch gewrappt (gibt 'viewer' zurück wenn SUPABASE_SERVICE_ROLE_KEY fehlt). Dashboard-Layout auf Promise.allSettled umgestellt (einzelne Query-Fehler crashen nie mehr alle /dashboard/*-Seiten). getOrganisationIdOrNull() in server.ts hinzugefügt (safe SSR-Alternative). getOrganisationId() nutzt maybeSingle() statt single(). Build sauber.
- S43: Digitale Signatur für Verträge (kein neues Migration nötig, Spalten in Migration 043 schon vorhanden). Neue öffentliche Seite /vertrag/[token] (VertragSignaturPage + VertragSignaturClient) – lädt Vertrag via signatur_token, zeigt HTML-Inhalt, Canvas-Unterschrift (touch + mouse), Bestätigungsseite nach Unterschrift. Neue Komponente SignaturCanvas.tsx (HTML5 Canvas, touch-optimiert, Wellbeing-Green Border, PNG Data-URL Export). Neue Server Actions in signatur.ts: vertragFuerSignatur/vertragUnterschreiben (adminClient, öffentlich), firmaUnterschreiben (Auth), signaturTokenErstellen (randomBytes 32, 30 Tage Gültigkeit). VertraegeClient erweitert: Signatur-Link Modal (Token erstellen, Link kopieren, Unterschriften-Status Kunde/Firma), Firma-Signatur Modal (SignaturCanvas in Modal). Middleware unverändert (/vertrag/* war bereits öffentlich).
- S44: 2D Raumplaner Schritt 1 – Migration 045 (3 neue Spalten auf raeume: grundriss_json/breite_m/laenge_m/hoehe_m; neue Tabelle moebel_symbole mit RLS; 19 System-Möbelsymbole als Seed-Daten). npm install fabric@6. Neue Server Actions raumplaner.ts (grundrissSpeichern/grundrissLaden/raumMasseAktualisieren/getMoebelSymbole). Neue Planer-Seite /dashboard/projekte/[id]/raeume/[raumId]/planer (Server Component). RaumplanerEditor.tsx (Client Component, Figma-ähnliches dunkles UI): Fabric.js Canvas mit dynamic import, After:render Grid-Overlay (Minor/Major-Raster), Zoom per Mausrad + Buttons, Pan per Space+Drag, Select/Wand/Radierer-Tool, Möbel-Sidebar mit Drag&Drop + Klick-Platzierung, Sucheingabe, Eigenschafts-Panel rechts (Objekt-Props oder Raum-Maße), Auto-Save debounced (2s), Undo/Redo History (50 Steps), Bottom-Statusbar (Mausposition in Metern + Zoom). "Raumplaner"-Button auf Raum-Detailseite. MoebelSymbol-Interface in types.ts, Raum-Interface um Raumplaner-Felder erweitert. (kein neues Migration nötig, Spalten in Migration 043 schon vorhanden). Neue öffentliche Seite /vertrag/[token] (VertragSignaturPage + VertragSignaturClient) – lädt Vertrag via signatur_token, zeigt HTML-Inhalt, Canvas-Unterschrift (touch + mouse), Bestätigungsseite nach Unterschrift. Neue Komponente SignaturCanvas.tsx (HTML5 Canvas, touch-optimiert, Wellbeing-Green Border, PNG Data-URL Export). Neue Server Actions in signatur.ts: vertragFuerSignatur/vertragUnterschreiben (adminClient, öffentlich), firmaUnterschreiben (Auth), signaturTokenErstellen (randomBytes 32, 30 Tage Gültigkeit). VertraegeClient erweitert: Signatur-Link Modal (Token erstellen, Link kopieren, Unterschriften-Status Kunde/Firma), Firma-Signatur Modal (SignaturCanvas in Modal). Middleware unverändert (/vertrag/* war bereits öffentlich).

- S45: Raumplaner Schritt 2 – Design + Performance + neue Features: RaumplanerEditor.tsx komplett überarbeitet. LoadingScreen-Komponente (animierter Fortschrittsbalken, Fade-out nach Canvas-Init). renderOnAddRemove: false + requestRenderAll für Performance. Autosave auf 3s erhöht. Kompakte h-10 Toolbar mit Tool-Indicator-Dots. MoebelPreview-Komponente (echte SVG-Pfade aus DB, korrekte Aspect-Ratio). ShortcutOverlay-Komponente (? Taste, alle Shortcuts gelistet). Neue Tools: Tür (arc-Gruppe aus Rect+Path, placeDoor()), Fenster (Glaslinien-Gruppe, placeWindow()), Maß (Distanzlinie mit Ticks+Label, finishMeasure()). Raumkontur: nicht-löschbares Rect aus Raummaßen (updateOutline(), data.type='outline', geschützt in Radierer+Delete). switchToolRef-Pattern gegen stale closures in Keyboard-Handler. Keyboard-Shortcuts: V/W/D/F/M/E für Tools, ? für Overlay. Canvas-Schatten um Editor-Bereich. Build sauber.

- S46: Raumplaner Polish + Speicher-Fix: KRITISCH – getCanvasJson() filtert Outline/Preview vor dem Speichern heraus; Laden filtert ebenfalls; Outline wird IMMER aus Raummaßen neu generiert (nie aus JSON). Autosave mit Fehler-State (rot "Fehler" statt grün). Toolbar neu: Tool-Gruppen mit Trennlinien, aktives Tool hat wellbeing-green Hintergrund + weißes Icon, neue Buttons Zentrieren (Maximize2) + Alle löschen (Trash2 mit confirm). Rechtsklick-Kontextmenü (Duplizieren, Ebene vor/zurück, Löschen). Möbel-Sidebar mit 5 klappbaren Kategorien (Wohnzimmer/Schlafzimmer/Büro/Küche/Bad). Status-Bar zeigt jetzt Anzahl Objekte + Grid-Größe + Tool-Name. Neue GrundrissVorschau.tsx (Client Component, read-only Fabric.js Canvas, fit-to-view, keine Interaktion). Raum-Detailseite: Grundriss-Vorschau-Block über Produkttabelle (mit Bearbeiten-Link) oder Platzhalter "Noch kein Grundriss" (mit Raumplaner öffnen). Build sauber.

- S47: Raumplaner komplett überarbeitet – Farben, Bugs, Toolbar, Grid, Pan: Gesamtes Farbschema auf Wellbeing-Green umgestellt (bg-[#1a2e1e]/[#2d3e31]/[#3a5240], border-[#445c49]/30, text-[#94c1a4]/[#c8dbc9]). Canvas-Hintergrund #e8ede9. Wand-Tool stoppt jetzt mit Rechtsklick + ESC + Doppelklick (stopWallTool/stopMeasureTool Callbacks, switchToolRef.current('select')). Ctrl+Scroll verhindert Browser-Zoom (preventBrowserZoom auf Window-Ebene, passive:false). Pan via Space+LMB UND mittlere Maustaste korrekt implementiert (allowTouchScrolling: false). Toolbar 48px hoch, Buttons 36×36px, Tool-Gruppen [Select,Wand] | [Tür,Fenster] | [Bemaßung,Radierer]. Grid-Dropdown ersetzt durch 3 Toggle-Buttons [10][25][50]cm. GrundrissVorschau: bg-[#e8ede9], max-height 300px, border+shadow. "Im Raumplaner bearbeiten" Button wellbeing-green. Build sauber.

- S48: Raumplaner FINALE – Pan, Zoom, Grid, PDF, Raumplaner-Übersicht: Pan komplett auf native DOM-Listener umgestellt (onMouseDown/Move/Up auf Container), mittlere Maustaste + Space+LMB zuverlässig. Zoom via `zoom *= 0.999 ** deltaY` mit zoomToPoint (smooth, kein Sprung). Grid in World-Koordinaten (`ctx.setTransform(vpt)`), skaliert korrekt mit Zoom. Grid-Buttons jetzt 4 Stufen [10][25][50][100]cm. Farb-Konstanten-Objekt `C` (toolbar #2d3e31, sidebar #354a3a). Canvas-Hintergrund #ffffff (reines Weiß). Ctrl+Scroll auf Window-Ebene blockiert ({passive: false}). PDF-Export (jsPDF dynamic import, A4 landscape, grüner Header, Canvas-Screenshot). NavSidebar: neuer Tab "Raumplaner" mit Grid3X3 Icon zwischen Produkte und Freigaben. Neue Seite /dashboard/raumplaner (alle Räume als 3-Spalten-Grid, Abschnitte "Mit Grundriss" / "Ohne Grundriss", Projekte-Filter unten). GrundrissVorschau: weißer Hintergrund (#ffffff), max-height 280px. Build sauber.

- S49: Raumplaner Mega Update – Auto-Fit, NavSidebar ausblenden, Fullscreen, Locking, Objekt-Panel, Custom Möbel, Favoriten, Snap-to-Grid: Auto-Fit beim Öffnen (fitToViewRef, 150ms delay). NavSidebar return null im Planer-Editor. Vollbild-Modus (F11 + Button, fullscreenchange Listener). Objekt-Locking (L-Taste, lockMovementX/Y/lockScalingX/Y/lockRotation, data.locked, Panel-Toggle rot). Erweitertes Objekt-Panel: editierbare Name/X/Y/Rotation Inputs (onBlur → applyObjProp), Lock-Button, Duplizieren. Custom Möbel: Migration 046 (custom_moebel Tabelle, RLS, Trigger), Server Actions getCustomMoebel/customMoebelErstellen, Modal mit Name/Kategorie/Maße/Farbe+Swatches+Live-Vorschau, CustomMoebelType Interface. Favoriten: localStorage Set<string>, toggleFavorit, ⭐-Sektion oben in Sidebar. Snap-to-Grid: Magnet-Button, snapToGridRef, Math.round in object:moving, Snap-Indikator in Statusbar. MoebelGrid + CustomMoebelGrid mit starredIds/onStar Props + Stern-Overlay. ShortcutOverlay: L/F11/Magnet Shortcuts ergänzt. Build sauber.

- S50: Raumplaner Kunden-Präsentation + QR-Code + Bild-Export: Migration 047 (freigabe_token UUID/freigabe_aktiv/freigabe_erstellt_am auf raeume). Server Actions: getRaumplanOeffentlich (Admin-Client, anonym zugänglich), getRaumFreigabeInfo, raumFreigabeAktualisieren. Neue öffentliche Route /raumplan/[token] (Server Component + RaumplanPraesentation.tsx Client Component: read-only Fabric.js Canvas, Zoom/Pan, PNG + PDF Download, Header mit Logo+Raumname+Projektname, Footer). Editor: Share2-Button in Toolbar (grün wenn aktiv), Freigabe-Modal (Toggle aktiv/inaktiv, Link anzeigen + kopieren, QR-Code mit react-qr-code 120px, Vorschau-Link). Editor: Bild-Export-Modal (PNG/JPG Format, 1x/2x/4x Auflösung, transparent/weiß Hintergrund, Grid temporär ausblenden). npm install react-qr-code. Build sauber.

- S51: Raumplaner Möbel + Texturen: Migration 048 (60+ neue Möbelsymbole in 8 Kategorien mit SVG Draufsicht-Pfaden; boden_textur + wandfarbe Spalten auf raeume). MOEBEL_GRUPPEN erweitert: 3 neue Kategorien Esszimmer/Garten/Wellness + erweiterte Key-Liste. Boden-Texturen (12 Optionen: Holz hell/dunkel, Parkett Fischgrät, Laminat, Fliesen weiß/grau/schwarz, Marmor, Beton, Teppich beige/grau) als HTML-Canvas-Patterns → Fabric.js Pattern-Fill auf Floor-Rect (data.type='floor'), beim Laden wiederhergestellt, aus getCanvasJson/updateObjCount herausgefiltert. Wandfarbe: 16 Vorschläge + Custom Color Picker → alle Wände gleichzeitig einfärben, Farben in grundriss_json persistiert. Server Action raumTexturenSpeichern. UI: neue Sektionen BODEN + WÄNDE im rechten Panel. Build sauber.

- S53: Raumplaner Business Features (6 Teile): Produkt-Verknüpfung (Möbel-Objekte mit Produkten aus der Bibliothek verknüpfen via data.produkt_id, Suche/Filter-Modal, Produktkarte im Panel mit Name/Art.Nr./Preis, Link öffnen, Verknüpfung entfernen via Link2Off). Kosten-Übersicht (Inline-Berechnung im rechten Panel, gruppiert nach Produkt, CSV-Export). Angebot erstellen (FileText-Button, sammelt Canvas-Produkte, raumplanAngebotErstellen Server Action, navigiert zu /angebote). Versionen (Migration 049 raumplan_versionen mit RLS, 6 neue Server Actions, GitBranch-Button, Version-Speichern-Modal mit Löschen-Option, Version-Dropdown-Loader). Notizen (Note-Tool N-Taste, StickyNote-Icon, Klick → Text-Modal, Doppelklick bearbeitet Notiz, Group[Rect+Textbox] gelb, Notizen-Liste im Panel). Vergleichs-Ansicht (ArrowLeftRight-Button ab 2 Versionen, V1/V2-Selektor, ladeVergleich via Promise.all, side-by-side GrundrissVorschau). Build sauber.
- S52: Raumplaner Planungs-Features: Automatische Maßketten (Ruler-Toggle in Toolbar): Room-Outline Breite/Länge + Wand-Segmente mit beschrifteten Dimlines (addDimLine-Helper, data.type='dimension', 400ms debounce). Flächen-Berechnung im rechten Panel (Raum-Info Sektion: Fläche m², Umfang m, Volumen m³, Möbelanzahl, nur wenn Maße vorhanden). Kollisionserkennung (TriangleAlert-Toggle): O(n²) Bounding-Rect-Überlapp, rote Overlay-Rects (data.type='collision', 300ms debounce), Zähler in Statusbar. Canvas-Events: object:added/removed/modified → triggerDimUpdateRef + triggerKollUpdateRef. useEffect für showDimensions/showKollision state-changes. 6 Raum-Templates Modal (RAUM_TEMPLATES-Array: Wohnzimmer/Schlafzimmer/Büro/Küche/Badezimmer/Esszimmer, platziert Rect+Text-Groups). Bild-Import Modal (FileReader → img → imp.Image, 50% Opacity, als Referenzebene). Templates + Import = 2 neue Toolbar-Buttons + 2 neue Modals. Build sauber.

- S54: Raumplaner PDF Fix, Ausrichten & Verteilen, Gruppieren, Legende: PDF-Export berechnet Bounding-Box aller sichtbaren Objekte, setzt Viewport temporär auf Content-Fit (PAD=40px), exportiert PNG, stellt Viewport wieder her – kein Beschnitt mehr. Objekte ausrichten: bei Mehrfachauswahl (ActiveSelection) zeigt rechtes Panel 6 Ausrichten-Buttons (links/mittig/rechts, oben/mittig/unten) + 2 Verteilen-Buttons (H/V, nur ab 3 Objekten). Gruppieren: Ctrl+G → `active.toGroup()` (data.type='group'), Ctrl+Shift+G → `active.toActiveSelection()`, Buttons im Panel. groupSelectedRef/ungroupSelectedRef gegen Stale-Closures in Keyboard-Handler. Automatische Legende: List-Button in Toolbar, insertLegende() sammelt alle Möbel-Objekte, zählt nach Name, baut Fabric-Group (weißes Rect, grüner Header, Textzeilen pro Möbeltyp mit Maßen), data.type='legend', platziert in sichtbarer Canvasfläche. ShortcutOverlay um Ctrl+G/Ctrl+Shift+G ergänzt. Build sauber.

- S55: Raumplaner Tür/Fenster-Varianten, Etagen, Stückliste & Kurven: TEIL 1 – Neue Sidebar-Sektion "TÜREN & FENSTER" mit 6 Türvarianten (Standard L/R/Doppel/Schiebe/Falt/Glas) und 6 Fenstervarianten (Standard/Breit/Bodentief/Dachfenster/Bogenfenster/Rundfenster), je mit SVG-Preview und spezieller Fabric.js-Gruppe, Klick- und Drag-Platzierung. TEIL 2 – Stockwerke/Etagen (Migration 050 raumplan_etagen, 4 Server Actions, Etagen-Tabs in Toolbar, AutoSave schreibt in aktive Etage). TEIL 3 – Stückliste (Sheet-Button, Modal mit Tabelle Nr/Bezeichnung/Maße/Menge/EP/Gesamt, CSV + XLSX Export via xlsx-Paket). TEIL 4 – Gebogene Wände (Kurven-Tool PenLine, Taste K, 3-Klick quadratische Bézier, Live-Preview, ESC/Rechtsklick abbricht, data.type='wall' wallType='curved'). Build sauber.

- S58: Raumplaner Layer-System (Ebenen verwalten): LAYERS-Konstante mit 6 vordefinierten Ebenen (Wände/Türen+Fenster/Möbel/Notizen/Maße/Legende). Layer-Panel im rechten Sidebar (Raum-Einstellungen, aufklappbar): Farb-Dot, Name, Objekt-Count, Eye/EyeOff-Toggle für Sichtbarkeit, Lock/LockOpen-Toggle (gesperrt = gold), Quick Actions Alle zeigen/Alle aus. toggleLayerVisibility/toggleLayerLock setzt visible/selectable/evented/lock* auf allen Canvas-Objekten des Typs. LocalStorage-Persistenz mit 500ms Delay für Canvas-Init. Statusbar: Indikator "N Ebenen ausgeblendet" (gelb, klickbar → Alle zeigen). Build sauber.
- S57: Raumplaner Versions-Modal & UI Polish: Versions-Dropdown in Toolbar durch schönen Button mit Badge-Zähler ersetzt. Neues Übersichts-Modal (Aktuelle Arbeit Karte, Versions-Liste mit Mini-Vorschau/Datum/Beschreibung/Laden+Löschen-Buttons, Empty-State, Neue-Version Footer-Button). Sub-Modal 'Neue Version speichern' mit Name, Beschreibung-Textarea und Canvas-Live-Vorschau. Vergleich-Button in Modal-Header integriert. getRaumplanVersionen gibt jetzt grundriss_json + beschreibung zurück; raumplanVersionSpeichern akzeptiert optionale Beschreibung. Farben aufgehellt (toolbar #3d5242, sidebar #3f5544, hover #4a6350). Zoom-% klickbar → Dropdown mit Presets 25–200%, Einpassen, Zurücksetzen. Build sauber.
- S56: Raumplaner Deep-Dive Audit & Bug Fix: 5 Bugs behoben – saveNow (Ctrl+S) ignorierte aktive Etage und schrieb immer in grundriss_json (jetzt via aktiveEtageIdRef korrekt in Etagen-Tabelle); onContextMenu-Handler stoppte Kurven-Tool nicht per Rechtsklick (ergänzt); Duplikat-Import 'Plus as PlusIcon' entfernt; loescheEtage in Etagen-Tabs eingebaut (× Button, nur sichtbar wenn >1 Etage); eslint-disable-Kommentar für loescheEtage entfernt. Build sauber.
- S59: Raumplaner Formen-Tool (Rechteck, Kreis, Linie, Pfeil): Neues 'shape'-Tool (Taste S). Shapes-Dropdown in Toolbar mit 4 Subtypen (rectangle/circle/line/arrow, Icons Square/Circle/Minus/ArrowRight). Klick+Drag-Zeichnen mit Live-Preview (Rect, Ellipse, Line, Path für Pfeil). Arrow-Pfeilspitze als Path mit data.x1/y1/x2/y2 für Redraws in mouse:move. Shapes in 'shapes'-Layer integriert (data.type='shape'). Form-Stil-Panel im rechten Sidebar (wenn Form ausgewählt): Füllfarbe (Toggle+7 Swatches+ColorPicker), Konturfarbe (6 Swatches+ColorPicker), Strichbreite (range 1-12), Deckkraft (range 10-100%) – alle Änderungen sofort auf Canvas-Objekt. Stale-Closure-Prävention via shapeSubtypeRef/shapeSettingsRef. Build sauber.
- S60: Raumplaner Bug Fixes (5 Teile): Grid auf screen-space drawing (identity transform) umgestellt → behebt Versatz auf HiDPI/Laptop-Displays. Wandfarbe: wandfarbeRef eingeführt; object:modified + Tür/Fenster-Snap reset nutzen aktuelle Wandfarbe statt hardcoded '#1e293b'; neue Wände erhalten ebenfalls die gewählte Farbe. Boden-Textur: floor-Rect nun selectable/moveable; auto-fit auf Wand-Bounding-Box statt 0,0; existierendes Rect wird nur geupdated statt neu erzeugt. Ebenen: toggleLayerVisibility/Lock auf funktionalen setState umgestellt (kein stale-closure-Risiko). Elektro: neue Kategorie in MOEBEL_GRUPPEN + 'elektro'-Layer + Migration 051 (10 Symbole: Steckdose/Doppelsteckdose/Lichtschalter/Deckenlampe/Wandlampe/Einbauleuchte/TV-Anschluss/Netzwerk-LAN/Rauchmelder/Thermostat). Build sauber. Migration 051 muss manuell in Supabase ausgeführt werden.
- S61: Raumplaner KRITISCHE FIXES (3 Teile): TEIL 1 – Grid komplett auf Fabric.js Line-Objekte umgestellt (data.type='gridLine', ±3000px Range, Minor/Major-Linien, sendObjectToBack via reorderLayersRef). Keine CSS-Background/after:render-Hacks mehr; canvas.backgroundColor='#ffffff'. createGridLinesRef wird nach Canvas-Init, nach jedem loadFromJSON, nach Etagen-Wechsel aufgerufen; Grid-Toggle blendet Lines aus statt zu löschen; Grid-Größe-Buttons rufen createGridLinesRef auf. SKIP/SKIP_COUNT/SKIP_PDF um 'gridLine' erweitert. PDF/Bild-Export blendet gridLines temporär aus (nicht true hardcoded restore). reorderLayersRef: sortiert _objects als [gridLines, floors, outlines, rest]. TEIL 2 – Responsive Toolbar: ToolbarDropdown-Komponente (open/clickOutside, items-Array). isCompactToolbar-State (window resize listener, <1400px). Kompakte Toolbar mit 5 Dropdowns (Zeichnen/Bearbeiten/Ansicht/Export) + Always-Visible Select/Wand/Radierer + Zoom + Speichern. TEIL 3 – Boden-Textur Platzierung: applyFloorTexture löscht immer altes floor-Rect + berechnet Bounding-Box neu aus Wänden (getBoundingRect(true) + WALL_THICKNESS/2 padding); floor ist nicht mehr selectable/evented; reorderLayersRef stellt Layer-Reihenfolge sicher. Build sauber.

- S62: Security-Hardening (vollständig): Multi-Tenancy-Bypass-Fix – alle update/delete Server Actions (35+ Operationen in 18 Dateien) um `.eq('organisation_id', orgId)` ergänzt: partner.ts (konditionLoeschen), angebote.ts (3 Fns), timeline.ts (2 Fns), vertraege.ts (5 Fns), raumplaner.ts (7 Fns + Import-Merge), kommunikation.ts (3 Fns), einstellungen.ts (2 Fns), onboarding.ts (5 Ops), zeiterfassung.ts (2 Fns), notizen.ts (2 Fns), logo-upload.ts (2 Fns), signatur.ts (2 Fns), produkte.ts (variantenDefinitionLoeschen). Open-Redirect-Fix: middleware.ts – `?redirect=`-Param validiert (startsWith('/') && !startsWith('//')). SSRF-Fix: api/scrape-product/route.ts – Auth-Check + isSafeUrl()-Blocklist (RFC-1918 + Metadata-Endpoint + non-http). PDF/CSV-Export Org-Scope: api/angebote/[id]/pdf, api/vertraege/[id]/pdf, api/projekte/[id]/export (letzter hatte gar kein Auth!). RaumHinzufügen-Bug: useFormState-Stale-Closure-Bug behoben – auf useTransition+useRouter+direktes Action-Await umgestellt (Name-Input, optionaler Typ-Dropdown, Inline-Toast). Build sauber.
- S63: Angebote-Button bestätigt (existierte bereits in Projekt-Toolbar). Standard-Vertragsvorlagen: neue Datei `vorlagen-seed.ts` mit 3 professionellen HTML-Vorlagen für Interior Designer (Interior Design Vertrag Standard/Angebot Standard/Auftragsbestätigung, alle mit Platzhaltern). `erstelleStandardVorlagen(orgId)` (via AdminClient, nur wenn noch keine Vorlagen) wird im auth/callback nach neuer Org-Erstellung aufgerufen. `standardVorlagenErstellenAction()` Server Action für manuelle Auslösung. VertragsVorlagenVerwaltung: "Standard-Vorlagen laden"-Button (Sparkles-Icon) im Empty-State prominent + in Toolbar wenn 0 Vorlagen. Build sauber.
- S66: Onboarding-System Erweiterung (PROMPTS 1–3): Migration 054 (onboarding_anfragen erweitert um typ/fortschritt/aktuelle_sektion/auto_save/gueltig_bis/projekt_id/kunde_id/letzte_aktivitaet + Status 'in_bearbeitung'/'abgelaufen'; onboarding_vorlagen erweitert um typ/einleitung_text/abschluss_text/logo_url/akzent_farbe/email_betreff/email_text/deadline_tage; neue Tabellen onboarding_dateien/onboarding_inventar/onboarding_prioritaeten mit RLS; Indizes). TypeScript-Typen komplett neu: OnboardingStatus+Typ erweitert, 9 neue OnboardingFrageTyp (slider/budget_verteilung/upload/inventar/prioritaeten/datum_rechner/entscheider_matrix/checkliste/rangfolge), OnboardingBedingtVon für Conditional Logic, neue Felder in OnboardingFrage/Vorlage, neue Interfaces OnboardingDatei/InventarItem/Prioritaet. Server Actions (onboarding-erweitert.ts): onboardingLinkErstellenV2/onboardingAutoSave/onboardingAbsendenV2/onboardingDateiSpeichern/inventarItem-CRUD/prioritaetenSetzen/vorlageErstellenV2/vorlageAktualisierenV2/kundeUndProjektAusOnboarding/getOnboardingStats (14 Actions). VorlagenVerwaltung erweitert: 4 neue Frage-Gruppen (Spezial: Upload/Inventar/Prioritäten/Checkliste/Datum-Rechner/Entscheider-Matrix + Rangfolge + Slider/Budget), ConditionalLogicEditor-Komponente (Bedingungslogik: frage_id+operator+wert), FrageEditor um Hilfetext/Slider-Config/Upload-Config/Budget-Kategorien/max_auswahl, VorlageEditorModal: 3 Tabs (Felder/Einstellungen/White-Label), Typ-Dropdown (neukunde/projekt/universal), Einleitung-/Abschlusstext/Deadline-Tage/E-Mail, Akzentfarbe-Picker mit Live-Vorschau. Build sauber. Migration 054 muss manuell in Supabase ausgeführt werden.
- S65: window.confirm() + alert() global ersetzt: neue Komponente `ConfirmModal.tsx` (3 Varianten danger/warning/info, isLoading-Support). 10 Komponenten umgestellt: ConfirmDeleteButton (useTransition), NotizBlock, FreigabeLinkKarte (2 Confirms), KundenPortalSection, BrandingEditor, TimelineView (EventModal), PartnerKonditionenBlock, ProjektAktionenButtons (Archivieren+Löschen), EinstellungenTabs (Team-Tab Mitglied deaktivieren). RaumplanerEditor: 3× confirm() → raumConfirm-State + ConfirmModal; 4× alert() → raumAlert-State + Toast-Banner. VorlagenVerwaltung: TypDropdown auf position:fixed + getBoundingClientRect umgestellt (behebt Clipping durch overflow-y-auto im Modal-Body). Build sauber.
- S64: Migration 053 (Angebote & Verträge Erweiterung): angebote um 10 Spalten erweitert (version, vorgaenger_id, zahlungsbedingungen, interne_notizen, erstellt_von, gesendet_am, angesehen_am, beantwortet_am, antwort_notiz, token). Status-Enum erweitert um 'angesehen' + 'ueberarbeitung'. vertraege um 14 Spalten erweitert (angebot_id, vertragsnummer, vertragstyp, version, startdatum, enddatum, kuendigungsfrist, gewaehrleistung_monate, interne_notizen, erstellt_von, gesendet_am, kunde_unterschrift_ip/name, firma_unterschrift_von). Status-Enum erweitert um 'zur_unterschrift', 'aktiv', 'abgeschlossen', 'gekuendigt'. 4 neue Tabellen: angebot_positionen (relationale Positionen mit ep_netto intern), vertrag_meilensteine (mit Status offen/in_arbeit/erledigt/abgerechnet), vertrag_anhaenge (für Angebot+Vertrag, MIN-BEZUG-Check), dokument_aktivitaeten (Audit-Log, anon-INSERT Policy für Kunden-Events). Funktion naechste_vertragsnummer(). Backfill-Block für bestehende Verträge. TypeScript-Typen komplett aktualisiert (AngebotStatus+VertragStatus erweitert, neue Interfaces AngebotPositionRow/VertragMeilenstein/VertragAnhang/DokumentAktivitaet/Vertragstyp). AngeboteClient + VertraegeClient: neue Status-Einträge + fehlende Pflichtfelder in Objektliteralen ergänzt. Build sauber. Migration muss manuell in Supabase SQL-Editor ausgeführt werden.

- S67: Onboarding-Ergänzungen (Migration 055): 5 neue Tabellen (onboarding_sektionen/budget_verteilung/entscheider/branding/checkliste) alle mit RLS org-scoped; onboarding_anfragen um zugangs_infos + vertrags_basis JSONB erweitert. 7 neue TypeScript-Interfaces in types.ts (OnboardingSektion055, BudgetVerteilung, Entscheider, Branding, Checklistepunkt, ZugangsInfos, VertragsBasis). vorlagen-seed.ts: erstelleStandardOnboardingVorlagen() mit 3 fertigen Vorlagen (Neukunden-Onboarding Standard 12 Fragen / Projekt-Privat 15 Fragen / Projekt-Gewerbe 18 Fragen) inkl. Upload/Slider/Prioritäten/Budget-Verteilung-Feldtypen. auth/callback: ruft neue Seed-Funktion bei Org-Erstellung auf. Build sauber. Migration 055 muss manuell in Supabase ausgeführt werden.

- S88: Freigabe — Kundenentscheidungen beim Link-Entfernen zurücksetzen + Block-Diagnose-Abschluss (direkt auf `main`, 1 Commit, keine Migration). Problem (User): nach Löschen aller Links blieb Test-Feedback (kunde_favorit/freigabe_kommentar/freigabe_status/kunde_menge) auf `raum_produkte` stehen. Fix: neuer Helper `entscheidungenFuerTokenScopeZuruecksetzen` (freigaben.ts) setzt für den Scope eines Tokens freigabe_status→ausstehend/kommentar→null/kunde_favorit→false/kunde_menge→null (admin_favorit bleibt); aufgerufen in `freigabeTokenZurueckziehen` + `freigabeTokenLoeschen` (Scope vorher geladen). Zusätzlich Action `projektKundenEntscheidungenZuruecksetzen(projektId)` + Client-Komponente `ProjektEntscheidungenReset` (Button „Alle Kundenentscheidungen zurücksetzen" + 2-stufige ConfirmModal) im Freigaben-Tab (für Altdaten). Belege (freigabe_einreichungen) bleiben unberührt. **Block-Gruppierungs-Diagnose (S-vorher) wieder entfernt** — sie hat bestätigt, dass die Blöcke server-seitig korrekt gebildet werden (struktur zeigte Stuhl=2/Übertopf=2 in `bloecke`); der Block-„einzeln"-Eindruck war Alt-Stand vor Mig 123 + Einfrieren. tsc+lint grün. CHANGELOG 2026-06-02.
- S87: Produkte aus der Bibliothek löschen (direkt auf `main`, 1 Commit, keine Migration — `produkte.deleted_at` existiert). Neue Action `produktAusBibliothekLoeschen(produktId)` (Soft-Delete, org-scoped, revalidate `/dashboard/produkte`; raum_produkte-Verknüpfungen bleiben, werden aber überall via `produkte.deleted_at` ausgeblendet → reversibel). `ProdukteTabelle`: Papierkorb-Button in Grid-Karte (absolut, hover) + Tabellenzeile (neue Aktionsspalte, colSpan 8→9), 2-stufige `ConfirmModal` (variant danger) mit Verwendungs-Warnung (`verwendetInAnzahl`), optimistisches Ausblenden via `geloeschtIds`-Set + Rollback/Fehler-Toast + router.refresh. Kompakt-Ansicht bewusst ohne Löschen. tsc+lint grün. CHANGELOG 2026-06-02.
- S86: Freigabe-Umbau Teil 4+5 — Belege durchblättern + PDF-Protokoll (direkt auf `main`, 1 Commit, keine Migration). **Teil 4**: neue Actions `freigabe-einreichungen.ts` (getFreigabeEinreichungen(projektId)/getFreigabeEinreichung(id), org-scoped, fail-safe []). Neue Client-Komponente `EingereichteFreigaben.tsx`: Liste aller Belege (Freigabe-Nr/Unterzeichner/Datum/Summen) + read-only BelegModal (useModal; Unveränderlich-Badge, Summen-Band, Positionsliste mit StatusPill/Kundenwahl/Kommentar/Menge/Preis, Footer Prüfsumme + Summe netto, PDF-Button). In Projekt-Detailseite (Promise.all + Freigaben-Tab nach FreigabeLinkKarte) eingehängt. **Teil 5**: neue API-Route `GET /api/freigaben/einreichung/[id]/pdf` (Auth + org-scope, jsPDF+autoTable, pdf-helpers wiederverwendet: Logo/Branding-Header, Titel „FREIGABE-PROTOKOLL" + Nr, Projekt/Unterzeichner/Zeitpunkt 2-spaltig, Positions-Tabelle mit farbiger Status-Spalte via didParseCell, Zusammenfassung + Freigegeben-netto, Unveränderlichkeits-Hinweis + SHA-256-Prüfsumme, Legal-Footer + Seitenzahl). tsc+lint grün. CHANGELOG 2026-06-02. **Freigabe-Umbau damit komplett (Teil 0–5).**
- S85: Freigabe-Umbau Teil 2+3 — Pflicht-Signatur + unveränderlicher Einreichungs-Beleg (direkt auf `main`, 1 Commit). **Migration 125**: neue Tabelle `freigabe_einreichungen` (append-only RLS wie freigabe_audit: SELECT+INSERT authenticated, kein UPDATE/DELETE; org-scoped; `lfd_nr` Freigabe 1/2/3 je Projekt; unterzeichner_name/abgesendet_am/allgemeiner_kommentar; positionen+summen JSONB; scope_*; content_hash SHA-256; 2 Indizes). **Types**: FreigabeEinreichung + FreigabeEinreichungPosition + FreigabeEinreichungSummen. **freigabeAbsenden** (freigaben.ts): nach den Live-Writes + vor freigabeAbschliessen wird ein eingefrorener Snapshot ALLER In-Scope-Positionen geschrieben (raum_produkte rich-load + Block-/Bereich-Namen-Maps; `effektiverVpNetto` je Position; status/kommentar/kunde_favorit/kunde_menge aus den frisch geschriebenen Werten; lfd_nr via count; content_hash via crypto.createHash). Komplett try/catch fail-safe (fehlt Mig 125 → Beleg übersprungen, Absenden läuft). **FreigabeAbschlussModal**: ein Namensfeld → zwei Pflichtfelder Vorname+Nachname (kombiniert), Hinweis „Name + Zeitpunkt werden verbindlich protokolliert". tsc+lint grün. CHANGELOG 2026-06-02. **Migration 125 muss in Supabase ausgeführt werden.** Verbleibend: Teil 4 (Belege durchblättern) + Teil 5 (PDF-Protokoll).
- S84: Freigabe-Umbau Teil 1 — Links beim Erstellen einfrieren („nicht verfügbar" gekillt) (direkt auf `main`, 1 Commit). `freigabeTokenErstellen` löst für scope_typ='auswahl' die Auswahl (Einzelprodukte + ganze Bereiche) sofort zu festen `raum_produkte`-IDs auf (neuer Helper `aufgeloesteAuswahlProduktIds`, block-first via `bereichVonRaumProdukt`) und speichert sie in `scope_ids`; mind. 1 Produkt Pflicht (sonst Fehler, kein leerer Link). `scope_bereich_ids` bleibt nur Anzeige-Metadatum. Kundenseite (`freigabe/[token]/page.tsx`): `auswahlMitBereich` erfordert jetzt `scopeIds.length===0` → bei gesetzten scope_ids IMMER der stabile `.in('id', scopeIds)`-Pfad; dynamische Bereich-Auflösung nur noch Fallback für Alt-Links ohne scope_ids; Auto-Sync-Block (mit dem `deleted_at`-Bug) entfernt; `scopeIds` let→const. Dynamische Auto-Erweiterung neutralisiert: `freigabeScopeAktualisieren` (keine Aufrufer) + `freigabeAuswahlScopeFuerRaum` (3 Aufrufer, Signatur bleibt) sind No-Ops (void-Params) — beseitigt die letzten `raum_produkte.deleted_at`-Fehler-Queries. **Migration 124** friert bestehende Auswahl-Links ein (`scope_ids := scope_ids ∪ Produkte der gewählten Bereiche`; nutzt `rp.bereich_id`, korrekt nach Mig 123). tsc+lint grün. CHANGELOG 2026-06-02. **Migration 124 muss in Supabase ausgeführt werden.** Verbleibende Teile: 2) Pflicht-Signatur, 3) unveränderlicher Einreichungs-Snapshot, 4) Belege durchblättern, 5) PDF.
- S83: Teil 0 des Freigabe-Umbaus — Block-/Gruppen-Konsistenz (direkt auf `main`, 1 Commit). Root-Cause-Fix für die vom User vermutete Block-Fragilität: doppelte Bereich-Quelle (`produkt_gruppen.bereich_id` vs `raum_produkte.bereich_id`) konnte driften → Produkte „Ohne Gruppe", leere Bereiche im Picker, fehlende Produkte im Bereich-Link. Jetzt Single Source of Truth: Block-Mitglieder folgen IMMER dem Block-Bereich. `produkt-bereiche.ts#produktGruppeZuBereichZuordnen` zieht Mitglieder-`bereich_id` mit; `produkt-gruppen.ts` `raumProduktZuGruppeZuordnen`/`alternativenHinzufuegen`/`bibliotheksProdukteAlsAlternative` setzen `bereich_id` = Block-Bereich beim Zuordnen/Insert/Update. **Migration 123** (ersetzt die nie gelaufene 122 — referenzierte `raum_produkte.deleted_at`, das es nicht gibt): A) verwaiste Blöcke aus Mitgliedern rekonstruieren (Favorit zuerst), B) Mitglieder an Block-Bereich angleichen — beides OHNE `raum_produkte.deleted_at`. tsc+lint grün. CHANGELOG 2026-06-02. **Migration 123 muss in Supabase ausgeführt werden.** Geplante Folge-Teile: 1) Link beim Erstellen einfrieren (nicht-verfügbar killen) + Backfill, 2) Pflicht-Signatur, 3) unveränderlicher Einreichungs-Snapshot (neue Tabelle), 4) Belege durchblättern (Freigabe 1/2/3), 5) PDF-Protokoll.
- S82: Budget-/Kalkulations-Bug behoben (direkt auf `main`, 1 Commit, gleiche Ursache wie Box 1/S79, keine Migration). `projekt-kalkulation.ts#berechneProjektKalkulation` lud `raum_produkte` in BEIDEN Selects (rich + safe-Fallback) mit `.is('deleted_at', null)` — `raum_produkte` hat keine deleted_at-Spalte → beide Queries scheiterten → rpRaw=null → raumProdukte=[] → alle Summen 0 (budgetVerbrauchtNetto/ek/marge/provision/ertragSummeNetto). Symptom: Budget-Auslastungs-Ring + ProjektGesamtuebersicht (EK/Marge/Provision/Ertrag/Summe) auf der Projekt-Detailseite zeigten 0 €. Fix: deleted_at-Filter aus beiden Selects entfernt (provision_typ/fix-Fallback für Mig 120 bleibt). Einzelpreise/Produkttabelle nie betroffen (andere Query). tsc+lint grün. CHANGELOG 2026-06-02. **Verbleibend (gleiche Ursache, fail-safe/minor, noch offen):** `freigaben.ts:192/201/249` (Auswahl-Scope-Auto-Extend).
- S81: Kundenwunsch/-entscheidung löschen mit 2-Stufen-Bestätigung (direkt auf `main`, 1 Commit, keine Migration). `freigabe.ts#freigabeZuruecksetzenAdmin` erweitert: setzt jetzt zusätzlich `kunde_favorit=false` (vorher nur freigabe_status='ausstehend' + freigabe_kommentar=null) → vollständiger Reset der Kundenentscheidung (org-scoped, wird auch von FreigabenTabelle genutzt). `SortableProduktTabelle`: neues optionales Row-Prop `onKundenwunschReset(id,name)`; Papierkorb-Button in der Kundenwunsch-Box (erscheint bei jedem `freigabe_kommentar`, inkl. „Andere Alternative gewählt"-Geschwistern) + „Kundenwahl zurücksetzen"-Button (RotateCcw) auf dem gewählten Eintrag ohne Kommentar. Parent: `resetTarget`/`isResetting`-State, `handleKundenwunschReset` (optimistisch: status→ausstehend/kommentar→null/kunde_favorit→false, Rollback bei Fehler, Erfolg/Fehler-Toast, router.refresh), `ConfirmModal` (variant warning, 2-stufig: Button → Bestätigen). Reset ist pro Eintrag (nicht block-weit). tsc+lint grün. CHANGELOG 2026-06-02. Außerdem: Temp-Diagnose der Bereich-Scope-Auflösung auf der Freigabe-Seite (?vorschau=1&debug=1) eingebaut, bestätigt korrekt (imScopeCount>0, raeumeMitInhalt>0 → „nicht verfügbar" war transient) und wieder entfernt.
- S80: Gewählte Alternative im Admin sichtbar (direkt auf `main`, 1 Commit, kein Migration/keine Action-Änderung — reine Anzeige). Problem: hatte der Kunde im Freigabe-Link eine andere Alternative als die Empfehlung gewählt, stand bei den nicht gewählten Block-Mitgliedern nur „Andere Alternative gewählt" (= `freigabe_kommentar`, gesetzt in `freigabe.ts#freigabeFavoritWaehlen` auf die Geschwister), ohne zu nennen, WELCHE. `SortableProduktTabelle`: neue Memo `kundeWahlProBlock` (Map produkt_gruppe_id → {id,name} des `kunde_favorit`-Eintrags aus `eintraege`), an `SortableProduktZeile` via neuem optionalem Prop `kundeWahl` durchgereicht. Row: (a) grünes Badge „✓ Kundenwahl" (CheckCircle2) auf dem gewählten Eintrag; (b) Kundenwunsch-Box ersetzt den Magic-String „Andere Alternative gewählt" durch „Kunde hat sich für eine andere Alternative entschieden: <Name>" (echte Kundenkommentare unverändert via whitespace-pre-line). tsc+lint grün.
- S79: Freigabe „Auswahl"-Scope-Picker Bugfix (direkt auf `main`, 1 Commit). Symptom: im Picker (FreigabeLinkKarte, Umfang „Auswahl") zeigte **jede** Gruppe „leer" und war nicht ankreuzbar, obwohl sie Auswahl-Blöcke mit Produkten enthielt. Root-Cause: `freigabeScopeOptionenLaden` (`freigaben.ts`) zählte Produkte je Bereich über eine **separate** `raum_produkte`-Query mit `.is('deleted_at', null)` — `raum_produkte` hat aber **keine** `deleted_at`-Spalte (Mig 101: harte Löschung) → PostgREST-Fehler → `ownBereich`/`blockOf`-Maps blieben leer → `anzahlProBereich` leer → alle Bereiche `anzahl:0` → „leer". Fix: den `deleted_at`-Filter aus dieser Query entfernt (genau wie `getRaumProdukte`/Kunden-View, die ihn nie hatten); block-first-Resolver `eff = ausBlock ?? ownBereich` unverändert, Counts stimmen jetzt 1:1 mit der Raum-Produkttabelle. `if (!error)`-Guard bleibt (fehlende Mig 114/116 → weiterhin sauber „0"). CHANGELOG 2026-06-02. **Sibling-Bug derselben Ursache noch offen (separate Box):** identischer Filter in `projekt-kalkulation.ts:155/163` (Budget/Marge/Provision → 0), `freigaben.ts:192/201/249`, `freigabe/[token]/page.tsx:120/128` (Letztere try/catch → still). Keine Migration nötig.
- S78: Kalkulations-/Provisions-Redesign (Branch `claude/clever-darwin-lHDcu`, 6 Commits). Ziel: Preise/Provisionen rechnen, anzeigen und vererben durchgängig zusammen. **Entscheidungen:** Provision = Trade-Provision auf VK netto (= **Ertrag**); `partner_konditionen` = Single Source of Truth; Vererbung ins Produkt via **manuellem Button** (nicht still). **Migration 120** (produkte: `provision_typ` CHECK prozent/fix, `provision_fix`, `einkaufsrabatt_prozent` + Backfill provision_typ='prozent') + **121** (`partner_konditionen.ist_standard` + Partial-Unique-Index `uniq_partner_kondition_standard` + idempotenter Backfill alt `partner.provisionsmodell/provisions_wert` → Standard-Kondition; Alt-Spalten bleiben, werden nicht mehr gelesen/geschrieben). **Types:** Produkt += provision_typ/provision_fix/einkaufsrabatt_prozent (optional), PartnerKondition += ist_standard, neue `ProvisionTyp`/`ResolvedPartnerKonditionen`, Partner-Legacy @deprecated. **Resolver** `src/lib/partner-konditionen.ts#resolvePartnerKonditionen(konditionen, kategorieName, am)` (rein; Kategorie-Key = **Name**; Prio kategorie_basiert→Standard→prozent_fix/gestaffelt(Basisstufe+Hinweis)/fix_pro_produkt; rabatt_einkauf/mindestbestellwert/fix_pro_bestellung; Zahlungsziel/Skonto) + Action `getResolvedKonditionenFuerProdukt`. **preise.ts:** `provisionBetrag({typ,prozent,fix}, vpNetto)` (Fix=€/Stk, sonst %; legacy null→%) + `einkaufNettoNachRabatt`. **Rollup** (projekt-kalkulation.ts): RaumKalkulation+ProjektKalkulation += ekSummeNetto/margeSummeNetto/provisionSummeNetto/**ertragSummeNetto (=Marge+Provision)**; fail-safe rich/safe-Select (provision_typ/fix optional). **Anzeige (nur intern):** ProjektGesamtuebersicht-Block (EK/Marge/Provision/Ertrag), Raum-Detail-Summen (effVP jetzt inkl. Rabatt = SSOT), SortableProduktTabelle Provision via provisionBetrag (fix/%-aware), CSV-Spalte „Provision gesamt (€)". **ProduktFormular:** Provision-Umschalter Prozent|Fix€, Einkaufsrabatt-Feld, EP=0-Guard (Marge→0 statt NaN), Hinweis-Panel + Button „Vom Partner übernehmen" + Abweichungs-Flag (useEffect lädt Resolver bei partner/kategorie-Wechsel). produkte.ts: 3 Felder in `neueFelder()` (alle Writer) + varianteAnlegen erbt. **PartnerKonditionenBlock:** Zeilen-Editoren für Staffelung + Kategorie-Werte (echte Kategorienliste, Key=Name) statt JSON-Textareas, Standard-Flag (Checkbox + Stern-Toggle + Badge, via `konditionAlsStandardSetzen`). **PartnerFormular:** „Einfache Provision" entfernt (Verweis auf Konditionen); partner.ts schreibt provisionsmodell/provisions_wert nicht mehr. **angebote.ts:** angebotAusProduktliste nutzt effektiverVpNetto (Rabatt-Fix). **Lieferantenbestellung:** Preis-Vorschlag = EK − Einkaufsrabatt (fail-safe Select). Durchgehend fail-safe (fehlende Mig 120/121 → leere/Default-Werte, kein Crash; neue Spalten nie in kritischen Pflicht-Selects ohne Fallback). tsc+lint+build je Commit grün. CHANGELOG 2026-06-01. **Migrationen 120 + 121 müssen in Supabase ausgeführt werden.**
- S77: Freigabe-Link auf `main` weiter ausgebaut. **(a) Admin-Vorschau:** Vorschau-Button (Auge) je Token in `FreigabeUebersicht` öffnet `/freigabe/<token>?vorschau=1`; `page.tsx` `searchParams.vorschau`-Gate, server-seitig nur für eingeloggte Nutzer DERSELBEN Org (`getOrganisationIdOrNull()===token.organisation_id`), umgeht Ablauf/Soft-Delete/PIN/Abschluss-Gates rein zum Testen, schreibt NIE (`vorschau`-Prop an FreigabeClient + FreigabeAbschlussModal: kein Entwurf/markieren, Absenden blockiert; gelber Vorschau-Banner Haupt- + Review-Screen). **(b) Review-Redesign:** `zeigeReview`-Screen neu (Kicker+Hero, Zusammenfassungs-Band Freigegeben/Änderung/Abgelehnt, Räume als Cards mit Thumbnails+Status-Pillen+Kommentar-Zitaten, sticky Absende-Leiste). **(c) Mehr Entscheidungsfreiheit (Migration 119):** Mehrfachauswahl in Auswahl-Blöcken (DROP unique-index `idx_rp_kunde_favorit_one_per_gruppe`; Client = Checkboxen via `toggleBlockMember`, mehrere `kunde_favorit` erlaubt; Completion-Gate „≥1 freigegeben/Block" unverändert), Wunsch-Menge (`raum_produkte.kunde_menge`, `MengeStepper` −/+ auf Block-Mitgliedern + Einzelprodukten, `ProduktState.menge`, Commit nur bei Abweichung, Admin-Badge „Kunde: X"), Block-Sammelnotiz (`produkt_gruppen.kunde_notiz`, Textarea pro Block, Admin „Kundennotiz" im ProduktGruppeHeader). `FreigabeEntscheidung.menge` + neue `FreigabeBlockNotiz`; `freigabeAbsenden(…, blockNotizen)` schreibt kunde_menge/kunde_notiz fail-safe (nur In-Scope-Blöcke). page.tsx lädt kunde_menge/kunde_notiz fail-safe nach; localStorage-Draft v2 (`{produkte,blockNotizen}`, Back-Compat). Types FreigabeProdukt.kunde_menge/FreigabeProduktGruppe.kunde_notiz/RaumProdukt.kunde_menge/ProduktGruppe.kunde_notiz (alle optional). tsc+lint+build grün. CHANGELOG 2026-06-01. **Migration 119 muss in Supabase ausgeführt werden.**
- S76: Freigabe „Gruppen"-Ebene über den Auswahl-Blöcken (7 Commits, Branch `claude/clever-darwin-lHDcu`, NICHT auf main). Ziel: mehrere Empfehlungen je „Gruppe" (Lounge-Ecke = Sessel-Block + Tisch-Block …). **Modell:** neue Ebene `produkt_bereiche` (UI „Gruppe") OBERHALB der bestehenden `produkt_gruppen` (relabelt „Auswahl-Block", 1-von-N **unverändert** inkl. der gehärteten Favoriten-/Freigabe-Logik S74/S75). **Migration 116:** Tabelle `produkt_bereiche` (org-scoped RLS, Realtime, updated_at-Trigger) + `bereich_id` auf produkt_gruppen (Block→Bereich) + raum_produkte (Einzelprodukt→Bereich, bei Block-Mitgliedern ignoriert) + `scope_bereich_ids` UUID[] auf freigabe_tokens. **Migration 116b (optional/opt-in):** Backfill bestehende produkt_gruppen→Bereiche (löscht Favoriten, soft-deletet Blöcke). **Types:** ProduktBereich, FreigabeBereich; bereich_id auf ProduktGruppe/RaumProdukt/FreigabeProdukt; bereiche auf FreigabeRaum; scope_bereich_ids auf FreigabeToken. **Actions:** neue `produkt-bereiche.ts` (CRUD + produktGruppeZuBereichZuordnen/raumProduktZuBereichZuordnen, 1:1 nach raum-gruppen.ts; Bereich-Zuordnung resettet KEINE Favoriten). **Geteilter Resolver `src/lib/freigabe-scope.ts`** (bereichVonRaumProdukt/istImAuswahlScope) — von page.tsx (Anzeige) UND freigabe.ts (Write-Guard) identisch genutzt (gegen IDOR). **Admin (SortableProduktTabelle):** zweistufiges Layout (BereichHeader → Blöcke + Einzelprodukte + „Ohne Gruppe"-Bucket), Zuweisung per Dropdown (kein zweites DnD-Level), „+ Gruppe"-Button, displayEintraege aus unified `layout`-Memo, fail-safe (`[]` → exakt wie bisher). **Kunde (page.tsx + FreigabeClient):** je Raum in Bereiche partitioniert (synthetischer Trailing-Bereich „Ohne Gruppe", jedes Item genau 1×), geführter innerer Bereich-Pager (Weiter/Zurück, ein Bereich pro Seite) unter den Raum-Reitern; ProduktGruppeKarte/ProduktKarte + freigabeFavoritWaehlen/StatusAendern **unverändert**. **Scope (freigaben.ts + FreigabeLinkKarte):** „Auswahl" kann ganze Gruppen ankreuzen (scope_bereich_ids, dynamisch zur Ladezeit aufgelöst → spätere Produkte automatisch dabei); ladeItemsImScope + Completion bleiben auf produkt_gruppe. **Sicherheit (freigabe.ts):** auswahl-Scope-Guard erlaubt zusätzlich Bereich-Mitgliedschaft (nur AUSWEITEN), via geteiltem Resolver. Durchgehend fail-safe (fehlende Mig 116 → leere Maps, kein Crash; neue Spalten nie im kritischen Token-/rpEintrag-Select). Jeder Commit tsc+lint+build grün. CHANGELOG 2026-05-31. **Migration 116 muss in Supabase ausgeführt werden (116b optional).**
- S75: Freigabe Pre-Launch-Audit & Härtung (3 parallele Code-Audits, direkt auf `main` gepusht). **BLOCKER behoben:** (1) Abschluss mit Auswahl-Gruppen war dauerhaft blockiert — nicht gewählte Alternativen (`ausstehend`) zählten als „offen". Jetzt gruppen-aware: eine Gruppe gilt als entschieden, sobald EIN Mitglied `freigegeben` ist. Umgesetzt in `freigaben.ts#ladeItemsImScope` (selektiert produkt_gruppe_id) + `freigabeAbschliessen` (erfuellteGruppen-Set) + Auto-Projekt-Status + `FreigabeClient` Zähler auf Entscheidungs-EINHEITEN (je loses Produkt 1, je Gruppe 1) + `raumOffenCount`. (2) `freigabe.ts#freigabeStatusAendern` prüft jetzt `abgeschlossen_am` + `deleted_at` (Manipulationsschutz nach Absenden) + Scope-Guard (raum/auswahl → kein IDOR über Projekt; raum_id mitselektiert) — analog `freigabeFavoritWaehlen`. **HÄRTUNG:** `freigabeZuruecksetzenAdmin` + `freigabeBulkStatusAendernAdmin` explizit `.eq('organisation_id', orgId)` (defense-in-depth, RLS schützt bereits); `freigabeAbschliessen` prüft Projekt↔Token-Org vor Seiteneffekten; `freigabeInvalidierenBeiProduktAenderung` setzt `kunde_favorit=false` mit zurück; öffentliche `page.tsx` `.single()`→`.maybeSingle()` (keine 500); 1-Produkt-Gruppen im Kunden-Link als Einzelprodukt (Partition `>=2`); `branding.custom_css` via `sauberesCss()` defensiv gestrippt. `pinPruefen` (`projekte.ts`): server-seitiger Brute-Force-Lockout (5 Versuche → 15 Min Sperre), **fail-safe** falls Migration 115 fehlt (liest Lockout-Spalten optional, sonst PIN-Check ohne Lockout). **Migration 115:** `freigabe_audit` append-only (FOR-ALL-Policy → SELECT+INSERT, kein UPDATE/DELETE → Audit unveränderlich; Bulk-Insert läuft über authenticated-Client, daher INSERT-Policy nötig) + `projekte.freigabe_pin_versuche/freigabe_pin_gesperrt_bis`. Token-Entropie bestätigt: `gen_random_bytes(32)` (256-bit). Typecheck+Lint+Build grün. CHANGELOG 2026-05-31. **Migration 115 muss in Supabase ausgeführt werden.**
- S74: Freigabemodul-Redesign — Gruppen + Favorit/Alternative (6 Phasen, Branch `claude/clever-darwin-lHDcu`). **Migration 114:** neue Tabellen `raum_gruppen` (Gruppen VON Räumen je Projekt, Navigation) + `produkt_gruppen` (Auswahl-Sets VON Produkten je Raum), neue Spalten `raeume.raum_gruppe_id` + `raum_produkte.produkt_gruppe_id/admin_favorit/kunde_favorit`, partielle Unique-Indizes (genau 1 Favorit pro Gruppe, clear-before-set), org-scoped RLS + Realtime. Favoriten bewusst als Booleans auf raum_produkte (Präzedenz custom_moebel.ist_favorit). **Types:** RaumGruppe/ProduktGruppe/FreigabeProduktGruppe + Erweiterung Raum/RaumProdukt/FreigabeProdukt/FreigabeRaum. **Actions:** `raum-gruppen.ts` (CRUD+zuordnen, Löschen löst Räume vor Soft-Delete), `produkt-gruppen.ts` (CRUD+zuordnen, Gruppenwechsel resettet Favoriten-Flags), `raum-produkte.ts#adminFavoritSetzen/Entfernen` (clear-before-set, fasst freigabe_status NICHT an, kein Audit), `freigabe.ts#freigabeFavoritWaehlen` (öffentlich via Admin-Client, Token-Validierung + Scope-Guard, kunde_favorit clear-before-set, **Freigabe-Kopplung + Audit über bestehendes freigabeStatusSetzen**, Geschwister→'ausstehend'). **Admin-UI P2:** SortableRaumListe nach raum_gruppe_id gebucketet (klappbare Gruppen, Anlegen/Umbenennen/Löschen via ConfirmModal, per-Raum Auswahlfeld, Drag innerhalb Bucket via eigenem DndContext). **Admin-UI P3:** SortableProduktTabelle gruppierte Render-Sektionen mit ProduktGruppeHeader, ⭐-Favorit-Toggle + Favorit/Alternative-Badge pro Zeile, Hover-Auswahlfeld, „+ Auswahl-Gruppe"-Toolbar, Regruppieren per Drag (single SortableContext über displayEintraege). **Kunden-UI P4:** freigabe/[token]/page.tsx lädt produkt_gruppen + Favoriten-Flags, partitioniert je Raum in gruppen[]+lose produkte[]; FreigabeClient ProduktGruppeKarte (Alternativen als Auswahl mit Empfehlung-Badge + „Ihre Wahl", Klick=Favorit=Freigabe, optimistisch), state um kundeFavorit erweitert. **P5 Ansicht pro Raum:** FreigabeClient Raum-Tab-Leiste + Vorheriger/Nächster-Pager (nur aktiver Raum gerendert, Fortschritt projektweit); FreigabeRaumPanel (Freigabe-Status/Raum, nach Räume-Gruppen geclustert) im Projekt-Tab Freigaben; FreigabenTabelle View-Label „Gruppen"→„Nach Projekt". **P6:** Konfigurator als veraltet markiert (Handbuch-Warning-Banner; KonfiguratorLinkKarte war eh nicht mehr gemountet; Route/Tabellen unangetastet). Sidebar-Badge unverändert (zählt schon distinkte Projekte mit offenen Freigaben). Typecheck+Lint+Build je Phase grün. CHANGELOG unter 2026-05-30. **Migration 114 muss manuell in Supabase ausgeführt werden.**
- S73: Aufgaben-/ToDo-System (Trello-Style Kanban) — 10 Sub-Commits direkt auf main. **Migration 102:** neue Tabellen `aufgaben` (mit assignee_user_id/assignee_kunde/sichtbar_fuer_kunde, kunde_id/projekt_id/raum_id/raum_produkte_id/bestellung_id Soft-FKs, status Backlog/In Arbeit/Review/Erledigt, prioritaet niedrig/normal/hoch/dringend, faellig_am, reihenfolge fuer Drag&Drop, checklist JSONB, anhang_urls JSONB, quelle/quelle_id mit Unique-Partial-Index fuer Idempotenz analog timeline_events, RLS org-scoped, updated_at-Trigger) + `aufgaben_kommentare` (mit ist_kunde-Flag fuer Portal-Replies). Storage-Bucket `aufgaben-anhaenge` (50 MB) mit Org-Pfad-Policies. Realtime-Publication. **Types:** Aufgabe + AufgabeMitDetails + AufgabeStatus/Prio/Quelle + AufgabeChecklistItem/Anhang/Kommentar. **Server-Actions (`src/app/actions/aufgaben.ts`):** getAufgaben(filter), getAufgabe(id), getAufgabenUeberfaelligCount, aufgabeAnlegen/Aktualisieren/Loeschen, aufgabeStatusAendern (setzt erledigt_am auto), aufgabeReihenfolgeAendern (Bulk fuer Drag&Drop), aufgabeChecklistAktualisieren, aufgabeAnhangHochladen + aufgabeAnhangSigniert (1h URL), aufgabenKommentarAnlegen + aufgabenKommentareAbrufen, **syncAufgabeAusQuelle** (idempotent, failsafe — analog timeline.syncAutoEvent) + **syncAufgabeAusQuelleAdmin** fuer anonyme Token-Routes. **Audit:** 5 neue AuditAktionen + 'aufgabe' AuditEntitaet. **UI Sub-Commit 3:** NavSidebar-Eintrag „Aufgaben" mit ListChecks-Icon zwischen Bestellungen und Chats; Badge zeigt ueberfaellige offene Aufgaben des aktuellen Users; dashboard/layout.tsx laedt aufgabenCount via Promise.allSettled. /dashboard/aufgaben Server-Page mit StickyPageHeader; AufgabenBoardClient: 4-Spalten-Kanban mit @dnd-kit (closestCorners, optimistisches UI), Filter-Pills (Alle/Mir/Heute/Diese Woche/Ueberfaellig), Quick-Add per Spalte (Enter), Cards mit Prio-Punkt+Faelligkeits-Badge+Projekt/Kunden-Zeile+Checklist-Counter+auto-Indikator. **Sub-Commit 4 — Detail-Modal:** AufgabeDetailModal mit 2-Spalten-Layout (Body + Sidebar), Inline-editierbarer Titel mit Auto-Save bei Blur, Status/Prio/Faelligkeit/Sichtbarkeit-Toggles im Sidebar, Inline-Checkliste mit Add/Toggle/Remove, Anhang-Upload + signierte Preview, Kommentar-Form, Loeschen+Schliessen, useModal-Hook fuer ESC+Focus-Trap. Cards im Board sind klickbar (sortable.isDragging-Guard verhindert Modal-Open beim Drag). **Sub-Commit 5 — Auto-Sync (3 Quellen):** reklamationen.ts (reklamationAnlegen/StatusAendern/Loesen/Loeschen — Aufgabe wird bei Mangel/Transportschaden in 'hoch' angelegt, bei 'geloest' auf erledigt, bei Loeschen entfernt); lieferanten-bestellungen.ts statusUebergang (bestaetigt/versandt → 'Lieferung empfangen: BS-...' Task mit Faelligkeit=liefertermin_geplant, geliefert → erledigt, storniert → loeschen, bestellungLoeschen → loeschen); onboarding.ts onboardingAbsenden (V1) + onboarding-erweitert.ts onboardingAbsendenV2 + kundeAusOnboardingAnlegen + onboardingLinkLoeschen. Vertrags-Meilenstein-Hook ausgelassen — braucht erst CRUD-UI fuer vertrag_meilensteine. **Sub-Commit 6 — Dashboard-Widget:** MeineAufgabenWidget in DashboardWidgets.tsx (mit MeineAufgabe-Type), Liste der offenen Aufgaben des Users mit Prio-Punkt+Titel+Projekt/Kunden-Zeile+Faelligkeits-Badge+Quick-Erledigt-Button (optimistisch mit Rollback), Empty-State 'Alles erledigt'. dashboard/page.tsx: Lade meineAufgaben (8, sortiert nach Faelligkeit, try/catch), ROW 2 auf 3 Spalten. **Sub-Commit 7 — Projekt-Tab:** ProjektAufgabenBlock mit Filter-Pills (Offen/Erledigt/Alle), Inline-Quick-Add mit projekt_id+kunde_id auto-verknuepft, Liste mit Erledigt-Checkbox, Klick oeffnet AufgabeDetailModal; getAufgaben({projektId}) parallel via Promise.all in projekte/[id]/page.tsx; neuer Tab 'Aufgaben' (ListChecks-Icon, Badge=Offen-Count). **Sub-Commit 8 — Portal:** portalAufgabenAbrufen/Erledigen/Kommentar-Actions in portal.ts (Berechtigungs-Checks via session.kundeId + assignee_kunde/sichtbar_fuer_kunde); PortalAufgabenSektion brand-aware Client-Komponente mit Erledigt-Toggle (optimistisch+Rollback), Expand/Collapse mit Beschreibung + Kommentar-Form pro Card, 'Nur zur Info'-Badge bei !assignee_kunde; Sektion 'Was du tun sollst' im Portal-Dashboard zwischen Aktivitaet und Quick-Actions, nur sichtbar wenn Aufgaben vorhanden. **Sub-Commit 9 — Realtime:** useRealtimeRefresh auf 'aufgaben'-Tabelle im AufgabenBoardClient (debounced 600 ms, router.refresh()), useEffect synct State mit initialeAufgaben-Props bei Server-Refresh; AufgabeDetailModal subscribed auf 'aufgaben_kommentare' mit Filter aufgabe_id=eq.{id} solange Modal offen, debounced 300 ms. Typecheck + Lint nach jedem Sub-Commit gruen. CHANGELOG-Eintrag unter 2026-04-26. Migration 102 muss manuell in Supabase ausgefuehrt werden.

- S72: Kunden-Detail-360°-Cockpit. **Backend:** neue Actions in `src/app/actions/kunden.ts` — `kundeStats(kundeId)` aggregiert Projekte-/Angebote-/Verträge-Counts+Summen+letzter Kommunikations-Kontakt in 4 parallelen Queries; `kundeProjekteMitStats(kundeId, inklArchiviert?)` lädt Projekte mit Räume-Anzahl, Freigabe-Stats (gesamt/freigegeben/ausstehend/abgelehnt) und VP-Netto-Budget. In `actions/timeline.ts`: `kundeEventsAbrufen(kundeId)` lädt Timeline-Events über ALLE Projekte des Kunden via `.in('projekt_id', projektIds)` und reichert sie mit `projekt: { id, name }` an. **Timeline-Komponente:** neuer Prop `showProjektBadge` + optionales `projekt`-Feld im Event-Typ; wenn aktiv, wird ein grauer Projekt-Badge neben dem Raum-Badge (grün) angezeigt — in Liste + Gruppiert-View. **Neue UI-Komponenten:** `KundeStatsBand.tsx` (4 KPI-Karten: Projekte/Angebote/Verträge/Letzter-Kontakt, weiß mit farbigem Icon-Badge blau/amber/emerald/grau, hover wellbeing-green-Border, date-fns `formatDistanceToNow` für Zeitabstand); `KundeProjektliste.tsx` (ersetzt alte einfache Namensliste — pro Zeile Status-Punkt + Name + Archiv-Badge + Meta-Zeile + Freigabe-Progress-Balken sichtbar ab sm + Budget-Kurz ab md + Deadline-Countdown „in 3 Tg." mit AlertTriangle bei überfällig); `KundeTimelineBlock.tsx` (Wrapper um Timeline-Komponente mit Projekt-Filter-Dropdown oben rechts — „Alle Projekte (N)" + einzelne Projekte mit Event-Count; showProjektBadge nur aktiv bei „Alle"; intern scrollbar mit maxHoehe='440px'). **Page-Umbau:** `src/app/dashboard/kunden/[id]/page.tsx` — alte `getProjekte`-Funktion entfernt, parallel 7 Datensätze geladen (projekteMitStats, notizen, portalUser, kommunikation, rolle, stats, kundeEvents); KundeStatsBand oberhalb Header, KundeProjektliste + KundeTimelineBlock in rechter Spalte vor KommunikationBlock. Import `Plus` + Type `Projekt` entfernt. Design: konsistent weiße Cards `border border-gray-200 rounded-xl shadow-sm`, Hover-Effekte `hover:border-wellbeing-green/40`, kein Hero-Gradient. 5 Commits + CHANGELOG-Eintrag unter 2026-04-22. Typecheck + Lint grün. Keine Migration nötig — alle Daten liegen schon vor.

- S71: Bestell-/Liefer-/Freigabe-Status pro raum_produkte. Root-Cause war, dass `produkte.bestellstatus`/`bestellt_am`/`liefertermin`/`lieferung_erhalten_am` und `produktstatus.status`/`kommentar` global pro Produkt lagen — derselbe Artikel in Raum A und Raum B teilten zwingend denselben Status. Auch der Timeline-Auto-Sync war betroffen: Unique-Index `(org, quelle, quelle_id)` mit quelle_id=produkt_id → nur **ein** Event global pro Produkt. **Migration 076:** neue Spalten auf `raum_produkte` (bestellstatus bestellstatus_enum, bestellt_am, liefertermin, liefertermin_bestaetigt, lieferung_erhalten_am; freigabe_status TEXT CHECK + freigabe_kommentar) + Backfill aus `produkte`/`produktstatus` + 3 Indizes. **Migration 077:** DELETE aller quelle=produkt/bestellstatus Events (alte produkt_id-Keys), werden beim nächsten Sync mit raum_produkte.id neu angelegt. **Types:** RaumProdukt-Interface erweitert. **Actions (produkte.ts):** `bestellstatusAendern(raumProduktId, ...)` + `produktDatumAktualisieren(raumProduktId, ...)` + `syncProduktTimeline(raumProduktId, ...)` — alle schreiben/lesen raum_produkte; quelle_id für syncAutoEvent ist jetzt raumProduktId. **Actions (freigabe.ts):** `freigabeStatusAendern(token, raumProduktId, ...)` + `freigabeZuruecksetzenAdmin(raumProduktId)` schreiben raum_produkte.freigabe_status/kommentar. **Reads migriert:** FreigabenTabelle/dashboard/freigaben/page.tsx (select raum_produkte.freigabe_status statt produkte.produktstatus-JOIN), SortableProduktTabelle (eintrag.bestellstatus/bestellt_am/liefertermin/lieferung_erhalten_am/freigabe_status statt p.*), dashboard/projekte/[id]/page.tsx Stats, Raum-Detail-Filter, API-Export `/api/projekte/[id]/export`, dashboard/layout.tsx Freigabe-Badge-Count. **Writes migriert:** FreigabeClient+Action, SortableProduktTabelle-Optimistic-Updates direkt auf eintrag.*, konfigurator.ts auswahlAlsFreigabeUebernehmen → update raum_produkte in allen Räumen des Projekts statt globalem produktstatus-Upsert. **Types:** FreigabeProdukt + FreigabeEintrag: `id` ist jetzt raum_produkte.id, neuer `produkt_id`-Feld für globale Produkt-ID. **Debug:** temporäre Event-Zähler + Event-Liste auf Raum-Detail-Seite wieder entfernt. **Toast-Feedback:** grüner „Timeline aktualisiert ✓"-Toast nach erfolgreichem Sync, roter Toast mit Supabase-Message bei Fehler (inkl. Hinweis „Migration 075 fehlt?" bei column-not-exists). produktstatus-Tabelle bleibt vorerst, Inserts beim Produkt-Anlegen neutral belassen. Typecheck + Lint grün. Migrationen 076 + 077 manuell in Supabase ausgeführt.

- S70: Onboarding-Dashboard-Redesign + 3 neue Standard-Vorlagen + Kunde-Verknüpfung. **Seed:** `vorlagen-seed.ts` auf korrekte OnboardingFrage-Feldnamen (titel/pflichtfeld/sektion_id/mehrfachauswahl/placeholder) umgeschrieben; 6 Standard-Onboarding-Vorlagen (Neukunden-Standard, Projekt-Privat, Projekt-Gewerbe, Kontaktanfrage, Projekt-Briefing bestehender Kunde, Raum-Bestandsaufnahme) via SeedFrage-Typ. **Server-Action:** `onboardingLinkErstellen(vorlage_id, kunde_id?)` — bei verknüpftem Kunde werden name/email/telefon vorausgefüllt. **Page:** `/dashboard/onboarding/page.tsx` lädt Kundenliste parallel und übergibt an `OnboardingTabelle`. **UI (OnboardingTabelle.tsx):** Hero-Band mit Gradient + Deko-Blob + 4 StatCards (Gesamt/Offen/Ausgefüllt/Abgeschlossen); Filter-Tabs-Row (Alle · Offen · Ausgefüllt · Abgeschlossen · Abgelehnt) mit Zähler-Badges und eigenem Empty-State bei leerem Filter; Karten-Liste mit Typ-Avatar links (`getTypInfo(typ)` → UserPlus/Briefcase/Layers mit eigenen Farben), Vorlagen-Badge + Status-Badge + Datum rechts, aktive Karte mit grünem Rand/Schatten. **LinkErstellenModal:** pro Vorlage Typ-Icon + Typ-Badge, optionaler Kunden-Dropdown wenn `typ === 'projekt'`, Sticky-Footer-Layout. **Fix:** Auto-Sync Timeline funktionierte nicht, weil `.upsert({..., onConflict})` mit Partial-Unique-Index (`WHERE quelle != 'manuell'`) von PostgREST nicht korrekt übersetzt wurde — auf manuellen SELECT + INSERT/UPDATE-Pattern umgestellt (`actions/timeline.ts#syncAutoEvent`), plus `router.refresh()` nach `bestellstatusAendern`/`produktDatumAktualisieren` in `SortableProduktTabelle.tsx` reaktiviert. **Fix:** Onboarding-Formular „Anfrage absenden"-Button brach still ab, weil `DynamischesFeld` nur 6 Feldtypen renderte und Pflicht-Validierung bei unbekannten Typen null-Werte fand; neue Renderer für email/telefon/url/ja_nein/bewertung/skala + Textarea-Fallback für komplexe Typen + ScrollIntoView auf erstes Fehlerfeld + sichtbare Fehlermeldung. **Fix:** Toggle „Für Kunde im Portal sichtbar" in TimelineView+RaumEventButton von absolut-positioniertem Kreis auf flex-justify-start/end umgestellt (war in manchen Browsern über das Label gerutscht). Typecheck + Lint grün. 5 Commits auf claude/update-font-styling-PR1Pg gepusht.

- S69: Timeline-Redesign komplett (Migration 075). **Phase 1 Schema:** 3 neue Spalten auf timeline_events (quelle TEXT CHECK manuell/produkt/bestellstatus/deadline/angebot/vertrag, quelle_id UUID, kunde_sichtbar BOOLEAN default true) + Unique-Partial-Index (organisation_id, quelle, quelle_id) WHERE quelle != 'manuell'. TimelineEvent-Type um die drei Felder erweitert. **Phase 2 Auto-Sync:** syncAutoEvent(quelle, quelleId, projektId, daten, {loeschen?}) in actions/timeline.ts — idempotenter UPSERT auf Unique-Constraint + DELETE-Variante. QUELLE_KUNDE_SICHTBAR_DEFAULT-Map (produkt/deadline/vertrag=true, bestellstatus/angebot=false). 5 Hooks eingebaut: produkte.ts#produktDatumAktualisieren (Liefertermin → Auto-Event, null → Löschen), produkte.ts#bestellstatusAendern (bestellt/geliefert), projekte.ts#projektAktualisieren (deadline → Meilenstein), angebote.ts#angebotStatusAendern (gesendet/angenommen), signatur.ts#signaturTokenErstellen (zur Unterschrift mit Gültigkeits-Range). Alle Hooks mit try/catch entkoppelt — Sync-Fehler crashen Haupt-Action nicht. **Phase 3 EventModal:** neue Felder abhaengig_von (Multi-Select-Checkbox-Liste über alle Events des Projekts) + kunde_sichtbar (Toggle mit Beschreibung) + Auto-Event-Badge (orange, „⚡ Auto-Event aus: {quelle}, wird beim Sync überschrieben"). **Phase 4 Gantt:** SVG-Overlay mit Bézier-Pfeilen für Abhängigkeiten (grün normal, rot gestrichelt mit Warn-Pfeilspitze bei Datumskonflikt); Drag&Drop via window.mousemove/mouseup-Listener, Tages-Snapping (32px = 1 Tag), handleEventMove verschiebt auch end_datum unter Erhalt der Dauer. Kaskaden-Toggle „Abhängige mitverschieben" – transitiver Walk über abhaengig_von. Auto-Events visuell separiert (gestrichelte Kontur statt solid, ⚡-Icon im Balken, ring-offset bei Meilenstein, kein Drag). **Phase 5 Raum:** Raum-Filter-Chip-Row in TimelineView (Alle · Projekt-Ebene · {Räume}) mit useSearchParams für `?raum=…`-URL-Param (Deep-Link vom Raum-Detail). Neuer Client-Component RaumEventButton (Modal mit Titel/Typ/Start/Ende/Kunde-sichtbar, raum_id vorausgefüllt) auf Raum-Detailseite. **Phase 6 Dashboard-Widget:** DeadlineEvent-Type + events-Prop an NaechsteDeadlines. Query lädt timeline_events der nächsten 30 Tage + überfällige der letzten 7 Tage, filtert im Code per (tageVerbleibend <= erinnerung_tage ?? 7). Gemerged mit Projekt-Deadlines zu einer sortierten Liste, Icons je Typ (Flag/Truck/Clock/Layers). **Phase 7 Portal:** portalProjektAbrufen filtert timeline_events auf kunde_sichtbar=true. Typecheck + Lint grün. Migration 075 muss manuell in Supabase ausgeführt werden.

- S68: Audit (4 Säulen) — 10 Commits direkt auf main. **Säule 1 Sicherheit:** Migration 068 (RLS + org-scoped Policies auf 5 Client-Portal-Tabellen + 5 Indizes, schließt Cross-Tenant-Leak). Public-Routes-Audit: alle 4 Routes (/freigabe, /einladung, /onboarding, /vertrag) folgen bereits Token-First-Pattern. Soft-Delete-Filter in 3 UPDATE-Actions (updateProduktPositionen, bestellstatusAendern, produktDatumAktualisieren) um `.is('deleted_at', null)` ergänzt. **Säule 2 Daten:** Migration 069 (sync_produkt_kategorie BEFORE-Trigger + Backfill, löst Kategorie-Dualismus ohne App-Code-Änderung). Migration 070 (kommunikation.raum_id FK + Index). types.ts: TimelineEvent + Kommunikation erhalten raum_id. profil.ts: benutzerNamenAktualisieren synct jetzt zusätzlich vorname+nachname in auth.user_metadata (nicht nur full_name); JSDoc dokumentiert Source-of-Truth-Regel. **Säule 3 Workflow:** vertragAusAngebot Action in vertraege.ts (nutzt angebot_id-FK aus Mig. 053) + FileSignature-Button in AngeboteClient. konfiguratorAuswahlZuAngebot Action in konfigurator.ts + Button in KonfiguratorLinkKarte SessionZeile. OnboardingTabelle: "Kunde + Projekt anlegen"-Button (mit raeume_erstellen=true) wenn projekt_name vorhanden. mail-templates.ts NEU mit 4 Funktionen (angebotVersand/vertragSignatur/freigabeLink/onboardingLink, einheitliches Layout + Brand-Farbe + escapeHtml). 4 Mail-Trigger: angebotStatusAendern (bei 'gesendet' auto), signaturTokenErstellen (auto nach Token), freigabeMailVersenden NEU (manuell), onboardingLinkVersenden NEU (manuell mit Empfänger-Email). **Säule 4 Qualität:** src/lib/hooks/useModal.ts NEU (ESC + Body-Scroll-Lock + Focus-Trap + Auto-Focus). A11y-Härtung ConfirmModal + ProduktZuweisenModal + ProduktHinzufuegenModal (role="dialog", aria-modal, aria-labelledby, aria-describedby, aria-label auf X-Buttons). Typecheck + Lint grün. Migrationen 068/069/070 müssen manuell in Supabase ausgeführt werden.

## Anweisung
Am Ende jeder Session den Session-Log mit einem kurzen Eintrag aktualisieren.

**CHANGELOG.md (dauerhafte Regel):** Bei JEDEM Commit der User-sichtbare Änderungen bringt (neue Features, UI-Änderungen, Workflows, Bug-Fixes mit sichtbarem Effekt, Migrationen), muss auch `/CHANGELOG.md` aktualisiert werden — 1-2 Stichpunkte in einfachem Deutsch unter dem heutigen Datum (`## YYYY-MM-DD`). Sektions-Titel optional (`### Bereich`). Technische Refactorings die der User nicht merkt (z.B. Datei-Split, Type-Ergänzungen, Lint-Fixes) NICHT eintragen. Das Datum wird aus `# currentDate` ganz oben in dieser Datei genommen (oder vom User-Prompt-Kontext falls verfügbar). Wenn der heutige Datumsblock noch nicht existiert, neuen `## YYYY-MM-DD` ganz OBEN unter der Einleitung einfügen. Der Tab „Änderungen" in den Einstellungen rendert diese Datei automatisch; Badge in Sidebar zeigt Neues seit letztem Besuch.
